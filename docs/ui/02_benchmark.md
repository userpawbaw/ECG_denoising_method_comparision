# 레퍼런스 분석 — 관습과 미감에서 오는 것 `[0 단계 산출물]`

> **왜 이 단계가 있나.** 이 과제의 화면 결정은 거의 전부 **자기 실측**에서 나왔다
> (구간 선정 D-17, 같은 y 축, 참값 겹치기, 레인 렌더러). 그래서 **관습과 미감에서
> 오는 것만 구조적으로 비어 있다** — 실험이 알려주지 않는 종류다.
>
> 형식은 `docs/31_demo_design_review.md` 를 따른다 — **발전 / 지적 / 추가** 3 갈래,
> 그리고 「근거 없이 그럴듯한 항목은 채우지 않고 `[근거 없음]` 으로 둔다」.
> 여기에 칸을 하나 더 뒀다: **「이 차용이 우리 수치를 실제보다 좋아 보이게 하나?」**

## 0. 무엇을 봤고 무엇을 안 봤나

| 갈래 | 봤나 | 출처 |
|---|---|---|
| **R-A** 표시 규약 (심전도 격자 · 감시장치 관습) | ✅ | 공개 표준 |
| **R-B** before/after · 다중 계열 비교 | ✅ | `dataviz` · `ui-ux-pro-max` `charts.csv` |
| **R-C** 실시간 · 관측 대시보드 | ✅ | `ui-ux-pro-max` `charts.csv` |
| **R-D·E·F** 레이아웃 · 타이포 · 반응형 | ✅ | `ui-ux-pro-max` 119 UX 가이드라인 |
| 상용 제품 화면 캡처 | ❌ **[안 했다]** | 스킬 두 벌이 규칙 형태로 덮었고, 3.3 이 적었듯 **화면 DB 가 메울 빈자리가 아니었다.** 필요해지면 `chrome --screenshot` 으로 더한다 |

---

## 1. R-A — 심전도 표시 규약 `[측정]`

### 표준

| | 표준 | 뜻 |
|---|---|---|
| 가로 | **25 mm/s** | 소칸 1 mm = **0.04 s** · 대칸 5 mm = **0.20 s** |
| 세로 | **10 mm/mV** | 소칸 = **0.1 mV** · 대칸 = **0.5 mV** |
| 빠른 기록 | 50 mm/s | 부정맥을 볼 때. 소칸 = 0.02 s |

격자가 이 규약을 지키면 **세어서 읽을 수 있다** — RR 간격, PR, QT. 임상의가 화면을
보는 방식이 그것이다.

### 우리 화면은 어떤가 — **격자가 장식이고, 속도가 화면 폭에 딸려 간다**

CSS 에서 1 mm = 96/25.4 = **3.7795 px** 다. `demo/index.html` 은 구간 길이(10/4/2 s)를
고정하고 캔버스를 화면 폭에 맞추므로, **속도는 폭이 정한다**:

| 화면 폭 | 구간 10 s | 구간 4 s | 구간 2 s |
|---|---|---|---|
| **1920** (확정 표시 장치) | **49.6 mm/s** ← 우연히 표준 50 에 근접 | 124 mm/s | 248 mm/s |
| **1440** | 36.9 mm/s | 92 mm/s | 185 mm/s |
| **1280** (최소 방어폭) | 32.7 mm/s | 82 mm/s | 164 mm/s |

**같은 「10 s」 칩이 화면마다 다른 속도를 만든다.** 표준과 맞는 것은 1920 에서
10 s 하나뿐이고 그것도 의도가 아니라 우연이다.

그리고 `mockup_expo.html` 의 격자는 **15 px 고정**이라 어떤 시간에도 대응하지 않는다.
`index.html` 의 세로선은 1 초 간격이라 눈금으로는 맞지만 **대칸/소칸 구분이 없다.**

세로는 더 멀다 — 진폭을 `ylim` 으로 정규화하므로 **mV 눈금이 아예 없다.** 은행은
`Int16 × scale` 로 담고 원 신호가 mV 이므로 **환산은 가능하다.**

### 판정

| | 항목 | 판정 |
|---|---|---|
| **발전** | 격자를 **규약에 맞춘다** — 대칸 0.2 s, 소칸 0.04 s | **채택.** 지금 격자는 장식이라 아무 비용 없이 의미를 얻는다 |
| **지적** | **구간 길이가 아니라 속도를 고정한다** | 칩을 「10 s · 4 s · 2 s」가 아니라 **「25 mm/s · 50 mm/s」**로. 화면에 보이는 시간은 폭이 정한다(1920 → 19.9 s / 9.9 s). 확대는 **한 박동 렌즈**가 따로 맡는다 (`docs/31` 1.2) |
| **추가** | 세로를 **mV 배수로 스냅** | `ylim` 을 임의값이 아니라 ±1.0 / ±1.5 / ±2.0 mV 로. 그래야 대칸 0.5 mV 가 의미를 갖는다. **「모든 칸이 같은 y 축」 규칙은 그대로 지켜진다** |

> **차용 경계 — 빌리면 안 되는 것이 여기 있다.**
> 감시장치는 IEC 60601-1-8 에 따라 **적색 = 고우선 알람 · 황색 = 중우선**을 쓰고,
> 파라미터마다 색이 고정돼 있다(ECG 녹색 · SpO₂ 청록 …). **우리는 이것을 빌리지
> 않는다.** CLAUDE.md 가 「임상·진단 능력을 주장하지 않는다 — 연구용 신호처리
> 시스템」이라고 못 박았고, 알람 색 체계를 그대로 쓰면 **화면이 의료기기라고
> 말하게 된다.** 격자는 읽는 방법이고 알람 색은 의학적 주장이다 — 전자만 빌린다.

---

## 2. R-B — before/after 와 다중 계열 비교

우리 질문은 「입력 ↔ 출력 ↔ 참값을 어떻게 나란히 보이나」다.

| | 항목 | 판정 |
|---|---|---|
| **발전** | **레인(facet)이 이미 옳은 해법이다** | `dataviz` 가 「too many series」의 해법으로 드는 것이 facet 이고, `card_core.js` 의 `drawLane()` 이 그것이다. **색 부담을 레인이 이미 덜고 있다**(UF-1) |
| **지적** | **`emphasis` 가 빠져 있다** | 「한 계열이 요점이고 나머지가 맥락이면 categorical 이 아니라 emphasis(하나만 강조, 나머지 회색)」. 우리 주장은 **「이 조건에서는 고전이 이긴다」**이므로 정확히 emphasis 인데, 지금은 모든 방법을 같은 무게로 그린다 |
| **추가** | **제거된 성분**(입력 − 출력) 뷰 | `docs/31` 1.3 이 이미 채택해 둔 것이고, 5.6 안전성 프로브를 눈으로 보이는 자리다. 비교 UI 의 관습(원본/처리본/차이 세 짝)과도 맞는다 |
| — | radar · scatter | **`[해당 없음]`** — 스킬이 비교 차트로 드는 형식이지만 우리 데이터는 시계열이다 |

---

## 3. R-C — 실시간 · 관측 대시보드

`ui-ux-pro-max` 의 `charts.csv` 에서 우리와 가장 가까운 두 항목:

| 항목 | 요구 | 우리 |
|---|---|---|
| **Real-Time Streaming** | Canvas/WebGL · 60~300 s 버퍼 · **일시정지** · 확대 · Reset | Canvas ✅ · 정지 ✅ · **확대는 구간 칩뿐** · Reset ❌ |
| **Anomaly Detection** | 「**일시정지 없는 실시간 맥락은 쓰지 말 것**」 | 정지 있음 ✅ |
| 둘 다 | **A11y fallback: 데이터 표 + 현재값 요약.** 키보드로 Pause/Resume · focus 로 값 · +/− 확대 | **표 뷰 없음**(U-12) · **호버/포커스 없음**(U-10) |

| | 항목 | 판정 |
|---|---|---|
| **지적** | **표 뷰로 가는 길이 화면에 없다** | `docs/90_results_d0.md` 가 사실상 표인데 화면에서 못 간다. 「모든 차트는 표 쌍둥이를 갖는다」 |
| **지적** | **호버·포커스로 값을 읽을 수 없다** | 「HTML 차트는 기본적으로 인터랙티브하다」. 다만 **툴팁이 값에 이르는 유일한 길이면 안 된다** — 표 뷰와 짝이다 |
| **추가** | 키보드 조작 | Pause/Resume · +/− · Reset. 박람회에서 **발표자**가 쓰는 경로이기도 하다 |

---

## 4. R-D · R-E · R-F — 레이아웃 · 타이포 · 반응형

`ui-ux-pro-max` 119 가이드라인에서 우리에게 걸리는 것:

| 심각도 | 규칙 | 우리 상태 |
|---|---|---|
| **Critical** | 「**고정 폭·고정 높이 상자에 글을 가두지 마라.** 내용이 높이를 정하게 하고 단위 없는 line-height 를 써라」 | 파형 칸이 **96 px 고정**(U-3). 1920×910 에서 화면의 다수가 빈다 |
| Medium | 「**일관된 모듈러 스케일**을 쓰고 임의 크기를 쓰지 마라」 | 8~10 단계 난립, 그중 넷은 1 px 이내(U-8) |
| High | 「본문 최소 16 px」 | 본문 14~15 px · 수치 12 px(U-5) |
| High | 「본문 대비 4.5:1」 | **`[미측정]`** — 색 검증기는 차트 표면 대비(3:1)를 재고 본문 대비는 안 잰다 |
| Medium | 「터치 타깃 사이 8 px 이상」 | 칩 간격 5~6 px |
| High | 「가로 스크롤 금지 · 고정 px 컨테이너 금지」 | `@media` **0 개**(U-9) — 다만 화면 검사는 세 폭에서 넘침 없음을 확인했다 |

| | 항목 | 판정 |
|---|---|---|
| **지적** | **파형이 고정 높이다** — Critical 규칙에 정면으로 걸린다 | 3 단계의 첫 목표. 세로 공간을 먹고 자라게 한다 |
| **지적** | **타이포에 스케일이 없다** | 모듈러 스케일을 정한다 — 단계 수는 **화면이 실제로 요구하는 역할 수**로 정한다(본문 · 수치 · 라벨 · 제목 · 눈금) |
| **추가** | **본문 대비를 재는 자를 늘린다** | 지금 검증기는 차트 표면 대비만 잰다. WCAG 4.5:1 은 글자용이고 별개다 |
| **추가** | 조작 요소 간격을 8 px 이상으로 | 관람객이 직접 만지는 것이 기본이다(`docs/ui/01_system.md` 7 절) |

---

## 5. 3 단계로 넘기는 것 — 순서

| 순 | 무엇 | 어디서 왔나 |
|---|---:|---|
| 1 | **파형이 세로를 먹고 자란다** | R-D Critical · U-3 |
| 2 | **속도를 고정하고 격자를 규약에 맞춘다** (25/50 mm/s · 대칸 0.2 s) | R-A |
| 3 | **세로를 mV 배수로 스냅**하고 대칸 0.5 mV | R-A |
| 4 | **`emphasis`** — 비교하는 둘만 강조, 나머지는 회색 | R-B |
| 5 | **타이포 모듈러 스케일** — 역할 수만큼만 | R-E · U-8 |
| 6 | **호버·포커스 · 표 뷰로 가는 길 · 시간축 라벨** | R-C · U-10 · U-12 · U-13 |
| 7 | 컨트롤 위계 · 8 px 간격 · 증거 수치 격상 | R-D · U-4 · U-5 |

## 6. 빌리지 않기로 한 것

| 무엇 | 왜 |
|---|---|
| **IEC 60601-1-8 알람 색 체계** (적/황/녹) | 화면이 **의료기기라고 말하게 된다.** 우리는 연구용 신호처리 시스템이다 (CLAUDE.md) |
| 파라미터 고정색 (ECG 녹색 · SpO₂ 청록 …) | 같은 이유. 우리 색은 **방법**에 고정된다(UD-3) |
| 감시장치의 매끄러운 렌더링 | `docs/31` 2 절의 경고 — **방법 간 차이가 시각적으로 뭉개진다.** 우리는 차이를 보이러 나온 화면이다 |
| radar · scatter 비교 형식 | 데이터가 시계열이다 |

## 7. 아직 `[근거 없음]` 으로 두는 것

- **모션·전환** — 스킬의 규칙은 「의미를 전달할 때만」 정도이고, 우리 화면에서 무엇을
  전환으로 보여야 하는지는 3 단계에서 화면이 정해진 뒤에 판단한다.
- **상용 제품 캡처** — 0 절. 규칙으로 덮인 부분이 많아 지금은 값이 낮다.

---

## 8. 레퍼런스 카드 — 시안 이전의 저비용 시각 정렬 (UD-10)

> 원본 `ecg-gui-design-review` `13_REFERENCE_GROUNDED_CREATIVE_MINING.md`(`08ffec3`)에서
> **규격만** 들였다. 그쪽 발견(F-004 「레퍼런스는 inspiration 이 아니라 **AI 와 사람이 같은
> 장면을 보는 대리물**이다」)이 이유다 — 텍스트 후보는 이해돼도 **느낌은 공유되지 않는다.**
> 후보마다 시안을 만드는 대신 실제 장면을 함께 본다.

### 8.1 카드 규격

```
REF-nn
출처 · URL
어디를 볼지          ← 필수. 「홈 열고 첫 0~5 초 reveal 만. 아래 섹션은 무관」처럼 장면·시간·스크롤·조작을 짚는다
잊히지 않는 순간
기제 (왜 먹히나)
기대 감각
프로젝트 번역        ← 구역(HIGH/MEDIUM/LOW) + 무엇을 절대 안 바꾸나(파형·시간축·단위·Reference/Difference)
복사 금지 목록       ← 원본의 팔레트·서체·오브젝트·셰이더 중 원리와 무관한 것
Imitation Distance   ← 0 복제 · 1 외형 · 2 레이아웃/기제 · 3 상호작용 원리 · 4 추상 경험 원리. 기본 3~4. 0~2 면 이유를 적는다
위험
최소 시안
장면 서술            ← **카드를 살리는 것은 이것이다.** 링크가 죽거나 사이트가 바뀌어도 남는다
L1 캡처(선택)        ← results/screens/refs/REF-nn.png. **.gitignore:108 로 커밋되지 않는다** — 라운드 중 사람이 보는 작업용이고, 제 3 자 화면을 공개 저장소에 싣지 않는다는 뜻도 된다
```

번역은 반드시 네 단계를 거친다: **레퍼런스의 특징 → 경험 원리 → 이 프로젝트에서의 뜻 →
구현 후보.** 원본 오브젝트를 그대로 가져오는 것은 기본값이 아니다.

### 8.2 소스 — 우리 범주가 먼저다

| 찾을 것 | 어디서 | 구역 |
|---|---|---|
| 기록지·계측기의 물성 (격자 · 보정 펄스 · 스타일러스) | 임상 기록지 실물 · 환자 모니터 · 오실로스코프 (R-A 가 이미 봤다) | LOW · MEDIUM |
| 과학 전시의 「들어감·확대·규모」 | NASA Eyes · 과학관 인터랙티브 · 데이터 저널리즘(NYT/Pudding) | HIGH · MEDIUM |
| 편집 타이포 · 큰 숫자 하나 | SiteInspire · Land-book 류 | MEDIUM |
| wow 3 초 · 시네마틱 전환 | Awwwards · Godly | **HIGH 만** |
| 상용 제품 흐름의 익숙함 | ~~Mobbin~~ **기각 유지**(UD-1) — 웹 조사로 대신 | — |

플랫폼 이름이 품질 보증이 아니다. 필요한 **경험 원리**에 맞는 장면을 고른다.

### 8.3 언제 — 자동 · 제안 · 생략

- **자동**: HIGH 구역(attract · Replay↔Live · 결과 reveal)의 **새 방향** · 사용자가
  「레퍼런스」「이런 느낌」「더 독창적으로」를 말할 때.
- **제안**(한 줄): MEDIUM 의 중요한 설계인데 방향이 아직 추상어일 때.
- **생략**: polish · 방향과 레퍼런스가 이미 고정된 구현 · 사용자가 「바로 구현」이라 한 때.

### 8.4 그 다음

카드 3~8 → 후보 5~8(각 후보에 REF 번호 · 빌린 원리 · 3 초 인상 · 위험 · 비용 · L0) →
사용자 정렬(카드를 **보고** 고른다) → 2~4 로 → 필요하면 브랜치 시안(`00_index` 「이중
디렉터」) → 검증기 넷 → KEEP/TUNE/REJECT → **UD**. 레퍼런스가 판단을 바꿨으면 UF,
워크플로를 골랐으면 UD, AI 를 쓰는 방법이 생겼으면 UR.

기각한 카드도 지우지 않는다.

---

## 9. 라운드 ① — Director A 브리프와 카드 (UD-11)

> **이 절이 Director A 의 작업 공간이다.** 브리프는 아래에 있고, 카드와 후보도
> 여기에 적는다. **`docs/ui/10_decisions.md` 를 열지 않는다** — 거기 Director B 의
> 발산이 들어 있고, 1 차 패스에서 그것을 보면 이 라운드의 독립이 깨진다(UD-11).
>
> 왜 로컬에서 도나: 이 저장소의 클라우드 세션은 외부 레퍼런스 호스트가
> **조직 egress 정책으로 403** 이다(UF-8). 우회하지 않고 환경을 바꾼다.

### 9.1 브리프

**대상**: `demo/ui/layout_b.html` 의 **attract / idle** — 아무도 안 만질 때의 화면.
지금 **없다**(`state.play` 는 `off|sweep|scroll` 셋뿐). 구역은 **HIGH**.

**무엇을 만들어야 하나**: 박람회 부스의 1920×1080 노트북. 3 m 밖에서 「저건 심전도고,
저 기계는 지금 돌고 있다」가 읽힌다. 다가오면 **만지지 않아도** 잡음과 결과가 갈리는
것이 보인다. 만지면 본 화면으로 이어진다.

**절대 안 바뀌는 것** (LOW 계약 — 표현을 위해 깨지 않는다):
파형 기하 · 시간축 · **50 mm/s · 10 mm/mV** · 단위 · Reference/Difference 의미 · 지표 의미 ·
「저장된 결과 재생 · 연구 시연 · 진단 용도 아님」 표기 · 무빌드(`file://`, 시연 중 계산 없음).

**이미 기각된 것** (되살리지 않는다): 파형 사이 모핑/크로스페이드 · 3D·원근 지표 블록 ·
배경 ECG 파티클·글로우 · v2 식 히어로 문구 + 자간 벌린 ALL-CAPS 눈썹 라벨 ·
진짜 임상 분홍 격자(`M04` 주황 · `M_FE` 분홍과 색상환이 겹친다) · 감열지 질감 텍스처.

**먼저 읽을 것**: `docs/ui/00_index.md` 「운영」 절(구역·증거 레벨) · 이 문서 §1 R-A
(표시 규약) · §6 「빌리지 않기로 한 것」 · §8 (카드 규격) · `.claude/skills/reference-mining`
이 없으므로 `.claude/skills/expo-ui-art-director` 와 `frontend-design` 을 쓴다.

**보는 것**: `python3 scripts/shoot_screens.py --only ui/layout_b.html --width 1920` 로
기준선을 찍어 **실제로 연다**. 기억으로 발산하지 않는다.

### 9.2 카드 — 여기에 적는다

§8 의 규격을 그대로 쓴다. **3~8 장.** 「어디를 볼지」와 **장면 서술**이 필수다 —
캡처는 `results/screens/refs/` 에 남기되 `.gitignore` 로 커밋되지 않으므로,
**글로 적힌 서술이 카드를 살린다.** Imitation Distance 기본 3~4.

_(비어 있다 — 로컬 세션이 채운다)_

### 9.3 후보 — 여기에 적는다

**5~8 개.** 후보마다: 목적 · 구역 · **REF 번호** · 빌린 **경험 원리**(외형이 아니라) ·
프로젝트 번역 · 기대 3 초 인상 · 구현 난이도 · 데이터/UX 위험 · validator 가 볼 것 ·
증거 레벨(보통 L0). 축이 한쪽으로 몰리지 않게: 구조 / 모션·reveal / 타이포·스케일 /
깊이·공간 / data-story.

낯설다는 이유로 빼지 않는다. **데이터 무결성을 직접 깨는 것만** 스스로 뺀다.
판정(KEEP/TUNE/REJECT)은 **하지 않는다** — 교차 검토 뒤 검증기 넷이 한다.

_(비어 있다 — 로컬 세션이 채운다)_

### 9.4 끝내기

1. `python3 scripts/check_records.py` — rc=0
2. `python3 -m pytest tests/test_repo_integrity.py -q` (전체 `pytest tests/` 가 돌면 더 좋다)
3. 커밋·푸시: 브랜치 `claude/wonderful-gates-0enr3f`. **검사와 커밋을 한 셸 줄에 두지
   않는다** — `pytest` 는 자기 줄에서 돌리고 요약 줄을 읽은 다음 호출에서 커밋(UO-2 ×2).
4. 끝나면 클라우드 세션에 알린다. 교차 검토와 검증기 넷은 거기서 돈다.
