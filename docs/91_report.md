# 91. 프로젝트 종합 보고서

> 대상: 이 실험을 처음 보는 평가자
> 전제 지식: 학부 신호처리 + 기초 기계학습
> 이 문서는 **왜 그렇게 설계했는가**에 초점을 둔다. 실행 방법은 `README.md`,
> 단계별 절차는 `docs/02_procedure.md`, 자동 생성 결과표는 `docs/90_results_<축>.md` 참조 (d0=합성, d1=MIT-BIH).

---

# 1. 문제 정의

## 1.1 출발점

자체 제작한 아날로그 프런트엔드(Arduino + AD8232 계열)로 ECG를 취득했으나,
아날로그 단에서 잡음을 줄인 뒤에도 잔여 잡음이 남는다. 오실로스코프 화면에서
광대역 잡음과 **순간적인 큰 spike**가 함께 관측되었다.

초기 접근은 Sameni의 model-based Kalman filtering이었으나 dB 개선이 기대에 못 미쳤고,
"우리 신호는 약 15 dB인데 논문은 0~10 dB에서 개선이 두드러진다"는 해석 하에
다른 기법을 탐색하려는 상황이었다.

## 1.2 문제를 다시 정의한 이유

위 상황에는 **검증되지 않은 전제가 세 개** 있다.

| 전제 | 검증했는가 | 실제 |
|---|---|---|
| "우리 신호는 15 dB다" | ✗ | clean reference가 없으면 SNR을 직접 계산할 수 없다. 정의부터 필요하다 |
| "Sameni 방식이 우리 조건에 안 맞는다" | ✗ | 구현이 올바른지 먼저 증명해야 한다 |
| "SNR을 올리면 좋은 것이다" | ✗ | 과도한 평활화가 SNR을 올릴 수 있다. 목표와 지표가 어긋난다 |

따라서 목표를 다음과 같이 다시 세웠다.

> **ECG morphology를 보존하면서 acquisition noise를 감소시키는 디지털 후처리 기법들을
> 동일한 조건에서 정량 비교하고, 실제 취득 환경의 잡음 조건에서 어떤 기법이 적합한지 규명한다.**

의도적으로 **하지 않는** 주장:
- "임상 진단 가능한 병원급 ECG를 복원한다" — 검증 불가능하고 규제 대상 주장이다.
- "의료기기를 개발했다" — 본 과제는 연구용 신호처리 시스템이다.

## 1.3 연구 질문

| RQ | 질문 | 답하는 실험 |
|---|---|---|
| RQ1 | 입력 SNR에 따라 어떤 기법이 유리한가? 특히 실측 조건에서는? | EXP-A |
| RQ2 | 잡음 **종류별**로 기법의 강약이 갈리는가? | EXP-B |
| RQ3 | 각 기법은 **깨끗한 신호에 해를 끼치는가**? | EXP-C |
| RQ4 | wavelet을 **전처리**로 쓰는 것과 **신경망의 표현공간**으로 쓰는 것 중 무엇이 나은가? | EXP-D |
| RQ5 | 딥러닝이 **없는 파형을 만들어내지는** 않는가? 부정맥을 훼손하지 않는가? | EXP-E |
| RQ6 | 공개 데이터로 학습한 모델이 자체 장비로 전이되는가? | EXP-F (실측 대기) |
| RQ7 | U-Net 의 **downsampling 이 QRS 를 뭉개는가**? 해상도를 유지하면 나아지는가? | EXP-A (M10 추가) |

---

# 2. 설계 원칙

구현에 들어가기 전에 네 가지 원칙을 정하고, 이후 모든 결정을 여기에 맞췄다.

## 원칙 1 — 평가를 기법보다 **먼저** 만든다

평가 코드를 나중에 만들면, 이미 구현한 기법의 결과를 보면서 무의식적으로
자기 기법에 유리한 지표와 조건을 고르게 된다. 그래서 순서를 강제했다.

```
PHASE 2 (평가 엔진)  →  PHASE 3 (고전 기법)  →  PHASE 4 (모델 기반)  →  PHASE 6-7 (딥러닝)
```

## 원칙 2 — baseline을 약하게 두지 않는다

"우리 기법이 X를 이겼다"는 주장은 X가 제대로 구현되었을 때만 성립한다.
초기 관찰이 "Sameni가 잘 안 된다"였으므로, **Sameni를 비교 실험에 넣기 전에
6항목 자가진단을 통과시키는 것**을 착수 조건으로 걸었다 (`docs/06_sameni_diagnosis.md`).

결과적으로 이 절차가 **구현 버그 2건**을 찾아냈고, 성능이 입력 5 dB에서
**+5.9 dB → +16.2 dB**로 바뀌었다. 이 검증이 없었다면 "Sameni는 우리 조건에서 무력하다"는
**틀린 결론**을 냈을 것이다.

## 원칙 3 — 상한선을 함께 그린다

"기법 A가 +8 dB를 냈다"는 말은 그 자체로 해석되지 않는다. 8 dB가 좋은 것인가?
더 짜낼 여지가 있는가? 이에 답하려면 **도달 가능한 최대치**가 필요하다.

| bound | 정의 | 답하는 질문 |
|---|---|---|
| `B01` oracle wavelet | clean 계수를 알고 각 wavelet 계수를 최적으로 keep/kill | wavelet thresholding으로 더 갈 수 있는가? |
| `B02` oracle Wiener | 참 PSD를 알고 `H(f)=Sx/(Sx+Sn)` 적용 | 선형 시불변 필터로 충분한가? |
| `M00` identity | 아무 처리도 안 함 | 처리가 도움이 되긴 하는가? |

두 oracle은 실제로 쓸 수 없다(clean을 참조한다). 그래서 이름이 `oracle_`로 시작하도록
코드에서 강제했고, 그런 방법만 `ctx['x_clean']`을 받을 수 있다.

## 원칙 4 — 재현 가능해야 한다

- 난수는 전역 seed가 아니라 `(experiment, record, window)`에서 **결정론적으로 유도**한다.
- 실험 1개 = YAML 1개.
- 모든 산출 디렉터리에 `manifest.json`(git hash, 패키지 버전, config 사본)을 남긴다.
- 표와 그림은 `results/`만 읽어서 재생성된다 — **원본 데이터에 다시 접근하지 않는다.**

---

# 3. 평가지표 — 왜 이것들인가

이 장이 보고서의 핵심이다. 지표를 잘못 고르면 이후 모든 실험이 무의미해진다.

## 3.1 "좋은 denoising"에는 서로 충돌하는 두 목표가 있다

clean ECG를 `x(t)`, 잡음 섞인 관측을 `y(t) = x(t) + n(t)`, 알고리즘 출력을 `x̂(t)`라 하자.

- **목표 A — 잡음 제거**: `x̂`에서 `n`의 흔적이 사라져야 한다.
- **목표 B — 신호 보존**: `x̂`가 `x`의 형태를 유지해야 한다.

두 목표는 충돌한다. 극단적으로, `x̂ = 0`은 잡음을 완벽히 제거하지만 신호도 없앤다.
반대로 `x̂ = y`는 신호를 완벽히 보존하지만 잡음도 그대로다.

ECG에서 이 충돌은 구체적이다. **QRS complex는 신호에서 가장 고주파 성분**이므로,
"고주파 = 잡음"이라고 가정하는 순간 QRS부터 깎인다. 실제로 본 실험에서
Savitzky-Golay 필터는 **깨끗한 신호에 통과시켰을 때조차 R-peak 진폭을 24.8% 감소**시켰다.

따라서 지표를 하나로 요약하려는 시도를 포기하고, **네 개 층으로 나눠 각 층이 서로 다른
질문에 답하도록** 설계했다.

```
층 1. 재구성 오차   — x̂ 가 x 에서 수치적으로 얼마나 벗어났는가
층 2. 형태 보존     — ECG 로서 중요한 구조가 유지되었는가
층 3. 하류 과제     — 이 신호로 실제 분석(심박 검출)이 되는가
층 4. 주파수 구조   — 어떤 주파수 성분을 없앴는가
```

---

## 3.2 층 1 — 재구성 오차

### 3.2.1 DC 규약 (모든 지표 공통)

**모든 지표는 `x`와 `x̂` 양쪽의 평균을 제거한 뒤 계산한다.**

이유: (1) ECG의 절대 전위 기준선은 임의값이고, 공통 front-end HPF가 이미 DC를 제거한다.
(2) 이렇게 해야 SNR/RMSE/PRDN/CC의 정의가 서로 모순되지 않는다.
느린 baseline wander 잔차는 평균 제거로 숨지 않으므로 평가력은 유지된다.

### 3.2.2 SNR을 **세 개**로 나눈 이유 — 본 프로젝트의 핵심 설계 결정

교과서적 정의는 이렇다.

```
SNR_in  = 10 log10( P(x) / P(y − x) )
SNR_out = 10 log10( P(x) / P(x̂ − x) )
SNR improvement = SNR_out − SNR_in
```

여기에 **함정**이 있다. 다음 세 종류의 방법은 **구조적으로 출력 진폭을 줄인다.**

| 방법 | 진폭이 줄어드는 이유 |
|---|---|
| soft threshold | `sign(d)·max(|d|−λ, 0)` — 살아남은 계수에서도 항상 `λ`만큼을 뺀다 |
| Kalman/EKS | process noise `Q` 설정에 따라 상태가 평균 쪽으로 눌린다 |
| MSE 학습 DL | 불확실한 구간에서 조건부 평균으로 수축한다 |

이 상태로 위 식을 그대로 쓰면, **"파형은 정확한데 크기만 3% 작은 출력"이
"파형이 3% 틀어진 출력"과 동일하게 처벌된다.** 방법 간 순위가 실제로 뒤집힌다.

그래서 세 값을 **항상 함께** 리포트한다.

| 이름 | 정의 | 답하는 질문 |
|---|---|---|
| `snr_out_strict` | `x̂` 그대로 | 그대로 써도 되는가 (실사용 기준) |
| `snr_out_scaled` | 최적 이득 `α* = ⟨x,x̂⟩/⟨x̂,x̂⟩` 보정 후 | 파형 구조 자체는 맞는가 (알고리즘 기준) |
| `gain_bias` | `α*` 값 자체 | 진폭이 얼마나 눌렸는가 |

**`gain_bias` 읽는 법**: `α* = 1.04`면 출력이 참값보다 약 4% **작다**는 뜻이다
(4% 키워야 오차가 최소가 되므로). 1보다 작으면 출력이 크다는 뜻이다.

**단위 테스트로 이 분리를 강제했다**: `x̂ = 0.5x`일 때
`snr_out_strict = 10log10(4) = 6.02 dB`, `snr_out_scaled = ∞`, `gain_bias = 2.0`.

#### 이 분리가 실제로 무엇을 잡아냈는가

`gain_bias` (1보다 크면 출력이 작다):

| 입력 SNR | `M00` identity | `M02` SavGol | `M05` EKS | `M04` SWT | `M07` |
|---|---|---|---|---|---|
| −5 dB | 0.241 | 0.723 | 0.832 | 0.692 | 1.014 |
| 20 dB | 0.990 | **1.035** | **1.043** | 1.000 | 1.004 |

- 20 dB에서 SavGol은 출력이 **3.5% 작고**, EKS는 **4.3% 작다**. 체계적 수축이 실재한다.
- `M04`(SWT garrote)는 1.000 — garrote를 고른 이유가 여기서 확인된다.
  (soft threshold를 쓴 초기 설정에서는 `gain_bias`가 1.6까지 갔다.)

`M00`(무처리)의 `gain_bias`가 −5 dB에서 0.241인 것도 중요한 정보다.
잡음이 신호보다 훨씬 크므로, 관측 신호를 0.24배로 줄이는 것만으로 오차가 줄어든다.
이것이 **스칼라 Wiener 이득**이고, 그 결과 `M00`의 `snr_imp_scaled`가 −5 dB에서
0이 아니라 **+6.2 dB**로 나온다.

> **해석 시 반드시 주의**: `snr_imp_scaled`는 identity에 무료 보너스를 준다.
> 그 크기는 `10log10(1+ρ) − SNR_in` (ρ = 선형 SNR)으로 정확히 계산되며,
> SNR이 높아질수록 0으로 수렴한다 (20 dB에서 +0.045 dB).
> **따라서 모든 곡선은 `M00` 선을 기준선으로 두고 읽어야 한다.**

### 3.2.3 RMSE와 PRDN

```
RMSE = sqrt( mean( (x − x̂)² ) )                            [mV]
PRDN = 100 · sqrt( Σ(x − x̂)² / Σ(x − x̄)² )                 [%]
```

- **RMSE**는 물리 단위(mV)라 직관적이지만 신호 진폭에 의존한다.
  진폭이 큰 기록과 작은 기록을 섞어 평균내면 큰 기록이 지배한다.
- **PRDN**은 신호 에너지로 정규화해 그 문제를 없앤다. ECG 압축/재구성 문헌의 표준 지표다.

문헌에는 `PRD1`(원신호 기준)과 `PRDN`(평균 제거 기준)이 혼재한다.
MIT-BIH 원신호에는 ADC offset이 얹혀 있어 `PRD1`은 실제 오차를 **항상 과소평가**한다.
**본 프로젝트는 `PRDN`만 쓰고 표기도 고정했다.**

**중요**: PRD는 흔히 "morphology 지표"로 소개되지만 **정확하지 않다.**
PRD는 정규화된 **전체 재구성 오차**다. morphology 왜곡이 그 오차에 반영될 뿐,
morphology를 직접 재는 것이 아니다. 그래서 별도의 형태 지표가 필요하다(층 2).

---

## 3.3 층 2 — 형태 보존

### 3.3.1 전체 파형 상관 `cc`

```
cc = Pearson correlation( x − x̄ , x̂ − x̂̄ )
```

RMSE와 상호보완적이다.

- RMSE는 **값의 정확성**을 잰다 (진폭이 절반이면 크게 벌한다).
- `cc`는 **모양의 유사성**을 잰다 (`x̂ = 0.5x`여도 `cc = 1.0`).

둘을 함께 보면 "모양은 맞는데 크기가 틀렸다"와 "크기는 맞는데 모양이 틀렸다"를 구분할 수 있다.

### 3.3.2 beat template 상관 `beat_cc` — 정렬 기준이 결정적이다

전체 파형 상관은 baseline 같은 저주파 성분이 지배해 **개별 심박의 형태 손상을 놓친다.**
그래서 R-peak를 기준으로 beat를 잘라 평균한 **template**끼리 비교한다.

여기서 **정렬 기준을 어떻게 잡느냐가 결정적**이다.

| 정렬 기준 | 문제 |
|---|---|
| `x̂`의 자체 검출 R-peak | 타이밍 오차가 상쇄되어 사라진다 → **부정확** |
| **clean reference의 R-peak (채택)** | 타이밍 오차가 상관에 반영된다 → 정확 |

본 실험은 **항상 clean reference의 R-peak로 양쪽 모두를 자른다.**

### 3.3.3 R-peak 진폭 오차 `r_amp_err_pct`

R-peak 근방 ±40 ms에서의 부호 있는 극값을 `x`와 `x̂`에서 각각 구해 상대오차를 계산한다.
±40 ms 창을 쓰는 이유는 **미세한 타이밍 이동과 진폭 감쇠를 분리**하기 위해서다
(타이밍은 `rpeak_mae_ms`가 따로 담당한다).

이 지표는 **과도한 평활화를 가장 직접적으로 잡아낸다.**

### 3.3.4 QRS duration 오차 `qrs_dur_err_ms`

NeuroKit2의 DWT 기반 delineation으로 QRS onset/offset을 찾아 폭을 비교한다.
QRS는 ECG에서 가장 빠르게 변하는 구간이므로, 공격적인 filtering이 QRS를 뭉개면
폭이 넓어지거나 좁아진다.

**단, 이 지표는 검출 알고리즘 자체의 불안정성을 포함한다.** 따라서 무조건 믿으면 안 되고,
아래 3.6의 "지표 자체의 분해능"을 먼저 재야 한다.

---

## 3.4 층 3 — 하류 과제(downstream task)

### 3.4.1 왜 필요한가

층 1·2는 모두 clean reference를 요구한다. 그런데 **실측 신호에는 clean이 없다.**
또한 궁극적으로 우리가 원하는 것은 "숫자가 좋은 파형"이 아니라
"실제 분석에 쓸 수 있는 파형"이다.

그래서 **denoising 결과에 심박 검출기를 돌려 그 성능을 잰다.**

```
Se  = TP / (TP + FN)      실제 심박을 얼마나 놓치지 않았는가
PPV = TP / (TP + FP)      검출한 것 중 진짜가 얼마나 되는가
```

### 3.4.2 공정성 규약

**모든 방법의 출력에 동일한 검출기(`wfdb.processing.xqrs_detect`)를 적용한다.**
방법마다 다른 검출기를 쓰면 최종 차이가 denoising 때문인지 검출기 때문인지 알 수 없다.

매칭은 **허용오차 ±75 ms 내 1:1 최소비용 매칭**(Hungarian)으로 한다.
그리디 매칭은 허용오차 안에 검출이 2개 들어올 때 지표를 부풀린다.

### 3.4.3 심박수 오차 `hr_err_bpm`

RR 간격의 **중앙값**으로 심박수를 구해 비교한다(평균은 이상치에 취약).
가장 직관적인 지표라 발표에서 유용하다.

---

## 3.5 층 4 — 주파수 구조

```
psd_logdist = mean( | 10log10 PSD(x) − 10log10 PSD(x̂) | )    [1–100 Hz]
band_power_err : 대역별 상대 파워 오차
```

**주의**: "ECG = 0.5–40 Hz, 그 위는 잡음"처럼 대역을 임의로 잘라 판정하지 않는다.
QRS에는 상당한 고주파 성분이 있다. **항상 reference PSD와 비교한다.**

PSD는 시간 영역에서 보이지 않는 것을 보여준다 — 어떤 방법이 60 Hz를 지웠는지,
어떤 방법이 QRS의 고주파까지 함께 잘라냈는지.

---

## 3.6 지표 자체의 분해능 — 이 단계를 건너뛰면 결과표가 무의미해진다

**방법 간 차이가 지표 자체의 흔들림보다 작으면 그 표는 아무것도 말하지 않는다.**

그래서 기법을 만들기 전에, clean 신호에 **무시할 수준의 교란(40 dB AWGN)**을 10개 seed로
주고 각 지표가 이상값에서 얼마나 흔들리는지 측정했다 (`docs/03_metric_floor.md`).

| 지표 | floor (p95) | 해석 |
|---|---|---|
| `rpeak_mae_ms` | ≈ 0 | 타이밍 계열은 해상도가 높다 |
| `hr_err_bpm` | 0 | 상동 |
| `qrs_dur_err_ms` | 0.64 ms | 이보다 작은 차이는 delineator의 불안정성 |
| `cc` | 5.0e-05 | 1에 붙어 있어 소수 4~5자리까지 봐야 한다 |
| `beat_cc` | 3.6e-07 | 매우 민감 |
| `psd_logdist` | **6.45 dB** | **매우 크다** |

**`psd_logdist`의 floor가 6.45 dB나 되는 것이 중요한 발견이다.**
ECG 파워가 거의 없는 주파수 구간에서 log-PSD 차이가 폭발하기 때문이다.
따라서 이 지표는 **절대값으로 해석하면 안 되고 방법 간 상대 비교로만** 쓴다.
PSD는 그림으로 보는 것이 주 용도다.

결과표에서는 floor보다 작은 값에 `*`를 붙여 **"구분 불가"**임을 명시한다.

---

## 3.7 ground truth가 없는 실측 신호용 지표

실측 Arduino 신호에는 clean이 없다. 그런데 "우리 신호가 정말 15 dB인가"에는 답해야 한다.
**답할 수 있다.** beat의 반복성을 이용한다.

### 3.7.1 세 가지 추정기

| 기호 | 방법 | 핵심 가정 |
|---|---|---|
| (A) | beat 평균 잔차: `m = mean(beats)`를 signal, `beat_i − m`을 noise로 | beat 형태 반복 + **잡음이 beat 간 무상관** |
| (B) | half-sample consistency: 홀수 beat 평균과 짝수 beat 평균의 상관 | 상동 (잡음 스펙트럼 가정 없음) |
| (C) | 등전위(TP) 구간 분산 | TP 구간이 평탄 + 잡음이 정상(stationary) |

(B)의 유도가 깔끔하다. 두 template을 `m + e_O`, `m + e_E`라 하면
(`e_O ⊥ e_E`, `P(e) = Pn/(N/2)`)

```
corr = Ps / (Ps + Pn')  = r' / (1 + r')      →   r' = corr / (1 − corr)
단일 beat SNR  r = r' / (N/2)
```

### 3.7.2 필수 전처리 — sub-sample 정렬

`fs = 250 Hz`에서 1 샘플은 4 ms다. QRS는 σ가 10 ms 수준이므로
**1 샘플 정렬 오차만으로 beat 잔차가 부풀어 SNR을 6 dB 이상 과소평가**한다.
실측: 참 SNR 20 dB인 신호를 **14 dB**로 추정했다.

→ 4배 업샘플 후 template 교차상관으로 정렬하는 것을 필수 단계로 넣었다.
수정 후 20 dB → **20.5 dB**로 추정된다.

### 3.7.3 가정 위반을 숨기지 않고 진단한다

(A)와 (B)는 잡음이 beat 간 무상관이라고 가정한다. 근전도 burst나 전극 움직임처럼
여러 beat에 걸쳐 상관을 갖는 잡음에서는 **SNR을 크게 과대평가**한다.

그래서 두 개의 진단 지표를 함께 낸다.

| 진단 | 의미 |
|---|---|
| `beat_resid_lag1_corr` | 인접 beat 잔차의 상관. > 0.15면 (A)(B)는 낙관적 |
| `artifact_beat_frac` | 형태 이상치 beat 비율. 크면 burst/임펄스형 → SNR을 단일 스칼라로 말하면 안 됨 |

실측 검증 (`docs/04_snr_estimator_calibration.md`): motion 잡음에서 `lag1 = 0.42`,
baseline wander에서 0.27로 **진단이 실제로 작동한다.** AWGN에서는 0.00이다.

또한 **잡음이 전혀 없어도 추정치가 무한대가 되지 않는다.** RR 변동에 따라 P/T 파 위치가
실제로 움직이므로 약 **22 dB에서 포화**한다. `ceiling_warning` 플래그로 알린다.

### 3.7.4 교정표를 미리 만들어 둔 이유

참 SNR을 아는 합성 데이터로 각 추정기의 편향을 미리 재 두었다.
그래야 실측값 "약 15 dB"를 정직하게 해석할 수 있다.
front-end 적용 후 AWGN 조건에서 편향은 **0.9 dB**다.

---

## 3.8 최종 지표 구성

**주 지표 5개** (결과표 본문)

| 지표 | 방향 | 답하는 질문 |
|---|---|---|
| `snr_imp_scaled` | ↑ | 잡음을 얼마나 제거했나 |
| `rmse` | ↓ | 참값에서 수치적으로 얼마나 벗어났나 |
| `cc` | ↑ | 파형 모양을 유지했나 |
| `rpeak_mae_ms` | ↓ | 심박 타이밍을 유지했나 |
| `qrs_dur_err_ms` | ↓ | QRS 형태를 유지했나 |

**보조 지표 8개**: `snr_imp_strict`, `gain_bias`, `prdn`, `r_amp_err_pct`,
`beat_cc`, `hr_err_bpm`, `psd_logdist`, `rtf`

**종합 점수를 만들지 않았다.** `0.4·SNR + 0.3·CC + 0.3·QRS` 같은 가중합은
가중치의 근거를 댈 수 없다. 대신 **Pareto 관점**으로 본다(4.4).

---

# 4. 실험 설계 — 왜 이 절차인가

## 4.1 신호 규격을 먼저 못박는다

| 항목 | 값 | 근거 |
|---|---|---|
| `fs` | **250 Hz** | Nyquist 125 Hz. 취득계가 이미 그 이상을 담지 못한다. 계산량 대비 최적 |
| window | **1024 = 4.096 s** | 정상 심박에서 4~6 beat 포함. `2^10`이라 SWT level 5가 안전 |
| hop | **512 (50 % overlap)** | Hann√ 창으로 정확 재구성 |
| 단위 | **mV** | R-peak 진폭 지표를 물리적으로 해석하기 위해 |

## 4.2 공정성 규약 5개 — 이것이 없으면 비교가 성립하지 않는다

### (1) 동일 입력

모든 방법이 **완전히 동일한 `y`**를 받는다. 평가 세트를 고정 시드로 미리 생성해 공유한다.

### (2) SNR sweep에서 잡음 실현을 고정

하나의 (record, segment, 잡음 조건)에 대해 잡음을 **한 번만** 뽑고 **스케일만** 바꾼다.
SNR마다 다른 잡음을 뽑으면 성능-입력SNR 곡선이 **'잡음 조성 변화'와 뒤섞여 해석 불가능**해진다.

### (3) 공통 front-end

```
FE1  zero-phase Butterworth HPF, 4차, 0.5 Hz     (baseline wander)
FE2  zero-phase Butterworth LPF, 4차, 100 Hz     (대역 제한)
FE3  IIR notch 60/120 Hz                          ← PSD로 PLI 존재를 판정한 뒤에만
```

- `filtfilt`(zero-phase)를 쓰므로 위상 왜곡이 없다 → R-peak timing이 밀리지 않는다
  (실측 편향 < 0.2 ms).
- **FE3를 무조건 적용하지 않는다.** 59–61 Hz 대역 파워가 인접 배경대역 중앙값의
  10배 이상일 때만 건다. 깨끗한 ECG의 하모닉 선구조에 오작동하지 않음을 확인했다
  (합성 ECG에서 ratio 1.0~1.6).

### (4) guard band — 실측으로 결정

zero-phase HPF는 경계에서 수 초간 링잉하고, Kalman은 수렴 시간이 필요하며,
wavelet/OLA는 패딩 경계 효과가 있다. 이를 방치하면 **깨끗한 신호에 front-end만 걸어도
왜곡이 11.9 dB**로 측정된다.

| guard [s] | front-end distortion [dB] |
|---|---|
| 0 | 11.5 |
| 1 | 25.9 |
| 3 | 40.9 |
| **5** | **41.9** (포화) |
| 10 | 41.7 |

→ **평가 구간 60 s, 양끝 5 s를 지표 계산에서 제외**로 확정.
**방법에는 전체 구간을 주고 지표만 내부에서 계산한다.** 모든 방법에 동일 적용.

### (5) 동일한 R-peak 검출기

모든 방법의 출력에 같은 검출기를 쓴다(3.4.2).

## 4.3 데이터 설계

| 세트 | 구성 | 용도 |
|---|---|---|
| **D0** 합성 | McSharry/Sameni ODE. 기록마다 다른 커널 | 파이프라인 검증, EKF 정답 검증, **외부 데이터 없이 전 실험 수행** |
| **D1** MIT-BIH + NSTDB | DS1/DS2 inter-patient split | 주 벤치마크 (외부 데이터 확보 후) |
| **D2** MIT-BIH clean + Arduino 잡음 | 장비 고유 잡음 합성 | device adaptation |
| **D3** Arduino 실측 | ground truth 없음 | 최종 real-world 평가 |

**본 보고서의 결과는 D0 기준이다.** D1은 외부 데이터 확보 후 동일 스크립트로 재실행한다.

### D0에 McSharry ODE를 쓴 이유

```
θ̇ = ω
ż = −Σᵢ (αᵢω/bᵢ²) Δθᵢ exp(−Δθᵢ²/2bᵢ²)
```

이 모델은 **Sameni EKF의 상태방정식과 정확히 같은 파라미터화**를 쓴다. 따라서
생성에 쓴 참 파라미터를 EKF에 그대로 주면 EKF는 이론적 최적에 도달해야 한다.
**도달하지 못하면 구현 버그다.** 즉 D0가 EKF의 정답 검증 케이스 역할을 한다.

### split 설계

- **record 단위 split** — window 단위로 나누면 같은 환자의 같은 morphology가
  train/test 양쪽에 들어간다.
- **잡음도 시간축 disjoint 분할** — NSTDB의 `bw/ma/em`은 각 30분짜리 기록 3개뿐이라
  랜덤 crop을 그냥 하면 train과 test가 같은 잡음 파형을 본다(noise leakage).

## 4.4 실험 매트릭스

| ID | 목적 | 규모 | 답하는 RQ |
|---|---|---|---|
| **EXP-A** | 입력 SNR sweep | 22 record × 2 seg × 6 SNR = **264 구간** × 12 기법 | RQ1 |
| **EXP-B** | 잡음 종류별 | 22 × 2 × 7 조건 = **308 구간** × 12 기법 (SNR 10 dB 고정) | RQ2 |
| **EXP-C** | distortion floor | 22 × 2 = **44 구간**, **잡음 없음** | RQ3 |
| **EXP-D** | wavelet 위치 ablation | EXP-A에서 M06/M07/M08 추출 | RQ4 |
| **EXP-E** | 안전성 프로브 | 22 구간 × 3 프로브 | RQ5 |
| **EXP-D2** | 시간 해상도 ablation | EXP-A에서 M06/M10 추출 (params·RF 정합) | RQ7 |

### EXP-C가 왜 필요한가 — 가장 저비용·고효율의 실험

**clean ECG를 그대로 각 방법에 통과시킨다.**

```
snr_distortion = 10 log10( P(x) / P(method(x) − x) )
```

이 값이 낮은 방법은 **잡음이 없어도 신호를 망가뜨린다.** 세 가지 의미가 있다.

1. delineator에 의존하지 않는 **가장 깔끔한 morphology 보존 지표**다.
2. **출력 SNR의 이론적 천장**이다. floor가 15 dB인 방법은 입력이 아무리 깨끗해도
   출력이 15 dB를 넘지 못한다.
3. 가로축 `snr_imp` × 세로축 `snr_distortion` 산점도가 곧 **Pareto front**다.

### 안전성 프로브(EXP-E) 설계

| 프로브 | 조작 | 실패 조건 |
|---|---|---|
| **P1** beat dropout | clean에서 1 beat(R ± 200 ms)를 등전위선으로 지우고 잡음을 덮음 | 그 자리에 QRS를 만들어내면 실패 |
| **P2** asystole | 3 s 구간을 등전위선으로 치환 | 심정지 구간에 beat를 생성하면 임상적으로 치명적 |
| **P3** ectopic | PVC beat만 골라 형태 보존을 정상 beat와 비교 | PVC가 정상 QRS로 '교정'되면 실패 |

`halluc_energy` = 지운 구간에서 출력이 만들어낸 에너지 / 원래 그 구간의 에너지.
**낮을수록 안전하다.**

## 4.5 통계 처리

- 집계 단위는 **record** (window는 서로 독립이 아니다 → p값이 과대해진다).
- 같은 record에 여러 방법을 적용했으므로 **paired Wilcoxon signed-rank**.
- 다중비교는 **Holm-Bonferroni** 보정.
- **효과크기(rank-biserial)를 함께 보고**한다. p값만 쓰면
  "통계적으로 유의하지만 0.2 dB 차이" 같은 무의미한 주장이 된다.

---

# 5. 결과와 해석

> **5.1~5.7 은 데이터축 D0(합성 ECG)** 의 결과다. TEST split 22 record,
> front-end 를 **모든 방법에 동일하게** 적용한 조건이다. 자동 생성 표·그림은
> `docs/90_results_d0.md`, 원본은 `results/d0/`.
>
> **5.8 은 D1(MIT-BIH 실기록 + NSTDB 실잡음)** 이고, 같은 방법을 다시 학습해
> 다시 측정한 결과다. **D0 의 결론 중 일부는 D1 에서 재현되지 않는다** —
> 어느 것이 왜 그런지가 5.8 의 내용이다.
>
> 초판은 딥러닝만 front-end 를 받지 못한 상태로 측정돼 있었다(F-10). 이 장은
> 그 결함을 고치고 전 모델을 재학습한 뒤 다시 측정한 결과이며, **초판의 주요
> 결론 두 개가 뒤집혔다.** 무엇이 왜 바뀌었는지는 5.7 에 따로 정리했다.

## 5.0 비교 대상

| ID | 분류 | 방법 |
|---|---|---|
| `M00` | — | Identity (하한선) |
| `M_FE` | frontend | **공통 front-end 만** (HPF 0.5 + LPF 100 + 조건부 notch) |
| `M01` | classical | Bandpass 0.5–40 Hz + 자동 notch |
| `M02` | classical | Savitzky-Golay (40 ms, 3차) |
| `M03` | time-freq | DWT soft threshold |
| `M04` | time-freq | **SWT adaptive threshold** (전역 σ + level별 k + QRS 보호 + garrote) |
| `M04np` | time-freq | `M04` 에서 QRS 보호만 끈 ablation |
| `M05f`/`M05` | model-based | **Sameni EKF / EKS** |
| `M06` | deep | **Residual 1D U-Net** (976 K params) |
| `M07` | hybrid | SWT → Residual U-Net (순차) |
| `M08` | hybrid | **Wavelet-subband Residual U-Net** (993 K params) |
| `M10` | deep | **Dilated ResNet** (980 K params, downsampling 없음 — `M06` 과 params·RF 정합) |
| `B01`/`B02` | oracle | Oracle wavelet / Oracle Wiener (**각자 자기 계열의 상한**) |

**`M_FE` 를 필수 행으로 넣은 것이 이 장의 거의 모든 발견을 가능하게 했다.**
각 방법의 이득 중 얼마가 공통 전처리에서 온 것인지 표 안에서 바로 읽힌다.

## 5.1 RQ1 — 입력 SNR에 따라 답이 바뀐다

`snr_imp_scaled` [dB], EXP-A 264 구간, record 단위 평균:

| 입력 SNR | `M00` | `M_FE` | `M01` | `M04` | `M05` | `M06` | `M07` | `M08` | `B01` |
|---|---|---|---|---|---|---|---|---|---|
| −5 | 6.16 | 9.12 | 9.95 | 10.46 | 11.17 | **17.25** | 17.17 | 17.02 | 13.42 |
| 0 | 3.00 | 7.18 | 8.03 | 8.98 | 9.39 | **15.57** | 15.33 | 15.55 | 12.07 |
| 5 | 1.19 | 6.18 | 6.56 | 8.21 | 7.47 | 13.46 | 12.74 | **13.65** | 11.06 |
| 10 | 0.41 | 5.64 | 4.99 | 7.71 | 5.22 | 10.80 | 9.58 | **11.37** | 10.11 |
| **15** | 0.14 | 5.09 | 2.88 | 7.03 | 1.84 | 7.63 | 6.00 | **8.71** | 8.91 |
| **20** | 0.04 | 4.26 | −0.04 | **5.86** | −2.27 | 3.86 | 1.91 | 5.51 | 7.31 |

**읽는 법**

1. **딥러닝이 −5 ~ 15 dB 전 구간에서 최고다.** 실측 장비의 추정 조건(≈15 dB)에서
   `M08` 8.71 dB 가 `M04` 7.03 dB 를 1.68 dB 앞선다.
2. **20 dB 에서만 `M04` 가 앞선다** (5.86 vs `M08` 5.51). 잡음이 거의 없으면
   신경망이 가하는 변형이 지우는 잡음보다 커지기 시작한다.
3. **딥러닝이 −5 ~ 10 dB 에서 oracle `B01` 을 넘는다.** `B01` 은 wavelet
   thresholding 계열의 상한이지 모든 방법의 상한이 아니라는 것의 직접 증거다.
   oracle 대비 gap 은 `M03`/`M04` 같은 같은 계열에만 적용할 수 있다.
4. `M01`(bandpass 0.5–40)은 20 dB 에서 **−0.04 로 무처리보다 나쁘다.** 40 Hz
   LPF 가 QRS 고주파를 잘라내는 손해가 잡음 제거 이득을 넘어선다.

### 통계 검정 (baseline = `M01`, paired Wilcoxon + Holm, n = 22 record)

| 방법 | Δ mean [dB] | p (Holm) | effect r | 판정 |
|---|---|---|---|---|
| `M08` | **+6.57** | 6e−6 | **1.000** | 유의 |
| `M06` | +6.03 | 9e−6 | 0.992 | 유의 |
| `B01` | +5.08 | 6e−6 | 1.000 | 유의 |
| `M07` | +5.06 | 9e−6 | 0.992 | 유의 |
| `B02` | +3.71 | 6e−6 | 1.000 | 유의 |
| `M04` | +2.65 | 6e−6 | 1.000 | 유의 |
| `M_FE` | +0.85 | 0.83 | 0.23 | **n.s.** |
| `M05` | +0.07 | 0.83 | −0.08 | **n.s.** |
| `M02` | −2.14 | 2e−5 | −0.97 | 유의하게 **나쁨** |

`effect r = 1.000` 은 **22개 record 전부에서** 이겼다는 뜻이다(rank-biserial 최댓값).

SNR 구간별로도 검정했다(78개 비교 중 55개 유의). 15 dB 에서 `M08` +5.83 (p<1e−4),
20 dB 에서는 `M04` +5.90 이 `M08` +5.55 를 앞선다.

**`M05`(Sameni EKS)가 `M01` 과 통계적으로 구분되지 않는다**(+0.07, p=0.83)는 것도
결과다. 구현을 두 번 고쳐(F-6) 진단 세트에서 +16.2 dB 를 냈던 방법이지만,
front-end 가 이미 적용된 입력에서는 추가 이득이 없다.

## 5.2 RQ2 — 잡음 종류가 답을 바꾼다 (가장 실용적인 결과)

입력 10 dB, `snr_imp_scaled` [dB]:

| method | bw | ma | em | pli | awgn | impulse |
|---|---|---|---|---|---|---|
| `M_FE` | 19.13 | 0.96 | 7.44 | 17.76 | 1.43 | 0.45 |
| `M04` | 19.13 | 7.62 | 7.44 | 19.55 | 8.00 | 0.45 |
| `M06` | 14.57 | 12.42 | 11.70 | 15.06 | 9.17 | 11.24 |
| `M08` | 16.78 | 12.78 | 12.56 | 17.46 | 8.88 | **11.98** |
| `B01` | 19.13 | 13.92 | 7.51 | 22.38 | 9.67 | 8.66 |

같은 표를 **`M_FE` 대비 순수 기여분**으로 다시 쓰면 구조가 드러난다:

| method | bw | ma | em | pli | awgn | impulse |
|---|---|---|---|---|---|---|
| `M04` | **−0.00** | +6.66 | **−0.00** | +1.80 | +6.57 | **+0.00** |
| `M06` | −4.56 | +11.47 | +4.26 | −2.70 | +7.74 | +10.79 |
| `M08` | −2.35 | **+11.82** | +5.12 | −0.29 | +7.45 | **+11.54** |
| `B01` | **−0.00** | +12.96 | +0.07 | +4.62 | +8.24 | +8.21 |

세 가지가 한꺼번에 보인다.

**(1) wavelet thresholding 은 bw·em·impulse 에서 구조적으로 0 을 기여한다.**
`M04` 뿐 아니라 **oracle `B01` 조차** 그렇다(bw −0.00, em +0.07). 구현 문제가
아니다 — 기저선 변동은 근사계수 `A5` 에 실려 있고 thresholding 은 세부계수만
건드리기 때문이다. **정답을 알아도 못 고친다.** 이 조건에서 관측되는 19.13 dB 는
전부 front-end 의 몫이다.

**(2) 딥러닝은 bw·pli 에서 손해를 낸다** (−0.29 ~ −4.56). front-end 가 이미
19 dB 를 해결한 뒤인데 신경망이 그 위에 무언가를 더 하려다 신호를 건드린다.
**잘 처리된 입력을 다시 처리하면 손해**라는 것이 수치로 나타난다.

**(3) 임펄스는 딥러닝만 풀 수 있다.** front-end 0.45, `M04` 정확히 0.00,
딥러닝 +11.5. `M08` 은 oracle `B01`(+8.21)마저 넘는다 — 임펄스 제거가 wavelet
계열이 다룰 수 있는 문제가 아니라는 뜻이다.

> **실용적 함의**: 단일 최선 방법을 고르는 것보다 **잡음 종류에 따라 처리를
> 선택하는 편이 낫다.** PLI 가 지배적이면 front-end 로 끝내고, 임펄스가 있으면
> 딥러닝을 쓰고, 광대역 잡음에는 둘 다 유효하다.

## 5.3 RQ3 — 깨끗한 신호에 해를 끼치는가 (EXP-C)

잡음 없이 clean 을 그대로 통과시킨 출력 SNR = **왜곡 하한**. 높을수록 무손실:

| 방법 | 왜곡 하한 [dB] |
|---|---|
| `M00` | ∞ (identity) |
| **`B02` / `M_FE` / `B01`** | **34.13 ± 3.72** (셋이 소수점까지 동일) |
| `M04` | 34.10 |
| `M03` | 32.40 |
| `M08` | 27.59 |
| `M06` | 25.04 |
| `M07` | 22.85 |
| `M01` | 22.26 |
| `M05` | 18.48 |
| `M02` / `M05f` | 15.58 |

**두 oracle 과 front-end 단독이 정확히 같은 34.13 dB 다.** oracle 은 정답을 알고
최적 복원을 하는데도 여기서 멈춘다 — **oracle 역시 front-end 를 통과하기
때문**이다. 즉 34.13 dB 는 어떤 방법의 성질이 아니라 **공통 front-end 가 정한
천장**이고, front-end 를 쓰는 모든 방법이 이 위로 갈 수 없다.

> 초판은 이 값을 보고 "`M04` 가 oracle 과 동일한 무손실"이라고 썼다. 절반만
> 맞다. `M04` 가 front-end 위에 0.03 dB 만 더 잃는 것은 사실이지만, **34 dB 라는
> 천장 자체는 SWT 가 만든 것이 아니다.**

`M01` 이 22.26 인 것은 40 Hz LPF 가 QRS 고주파를 잘라내기 때문이고,
`M02`(Savitzky-Golay) 15.58 은 진폭 오차 26.2 % 를 동반한다.

### morphology 는 SNR 에 따라 부호가 뒤집힌다

`qrs_dur_err_ms`(낮을수록 좋음, 지표 floor = 0.64 ms):

| 입력 SNR | `M06-L3` | `M_FE` | 판정 |
|---|---|---|---|
| 0 dB | 15.92 | 18.11 | **딥러닝이 개선** |
| 10 dB | 13.06 | 9.31 | 딥러닝이 악화 |
| 20 dB | 11.83 | 3.79 | **딥러닝이 크게 악화** |

저 SNR 에서는 잡음 자체가 morphology 측정을 망치므로 딥러닝이 잡음을 지우면
형태도 정확해진다. 고 SNR 에서는 지울 잡음이 없어 **딥러닝이 가한 변형만 남는다.**

전 SNR 평균으로 보면 `M_FE` 12.43 ms 가 `M06` 17.48, `M07` 21.52 보다 좋고
`M08` 13.97 과 비슷하다. `M05` 는 30.56 ms 로 가장 나쁘다.

> **결론**: 딥러닝은 잡음을 확실히 더 지우지만 **파형을 미묘하게 바꾼다.**
> 잡음이 적은 조건에서는 그 변형이 이득보다 크다. SNR 지표만 보면 이 사실이
> 보이지 않는다.

## 5.4 RQ4 — wavelet을 어디에 쓸 것인가 (EXP-D)

| 배치 | 방법 | 전체 평균 [dB] | −5 dB | 20 dB |
|---|---|---|---|---|
| 쓰지 않음 | `M06` | 11.43 | 17.25 | 3.86 |
| **전처리로** | `M07` | 10.46 | 17.17 | 1.91 |
| **표현공간으로** | `M08` | **11.97** | 17.02 | **5.51** |

**표현공간으로 쓰는 쪽(`M08`)이 전처리로 쓰는 쪽(`M07`)을 이긴다** (+1.51 dB).
`M07` 은 `M06` 보다도 낮다 — SWT 전처리가 신경망에 도달하기 전에 정보를 일부
없애고, 신경망은 그것을 되살릴 수 없다. 특히 20 dB 에서 `M07` 1.91 vs `M08` 5.51 로
격차가 크다.

### 손실 함수 ablation (STEP 19, TEST split)

**`M06` — L2/L3 가 L1 을 지배한다 (trade-off 없음)**

| | SNR 20 dB | QRS 폭 오차 | `beat_cc` |
|---|---|---|---|
| `M06-L1` (MSE) | 4.37 | 13.99 ms | 0.994 |
| `M06-L2` (+MAE) | 5.92 | **10.50 ms** | 0.996 |
| `M06-L3` (+diff) | **6.27** | 11.83 ms | **0.997** |

SNR 도 오르고 QRS 오차도 준다. 효과의 대부분은 **MAE 항**이 내고 미분항의
추가 이득은 작다.

**`M08` — L4 는 SNR 을 얻고 파형을 잃는다**

`M08-L1` → `M08-L4` 에서 SNR 20 dB 가 6.10 → 7.01 (+0.90) 로 오르지만
`beat_cc` 가 0.997 → **0.978** 로 떨어진다(floor 3.6e−7 의 5만 배). QRS 폭은
오히려 개선(8.75 → 8.07 ms)되는데 파형 상관은 나빠지는 **모순된 신호**다 —
폭은 맞추면서 형태를 바꾸고 있다.

> `snr_imp_scaled` 하나만 봤다면 "L4 가 최고"라고 썼을 것이다. DoD 가
> morphology 지표를 함께 요구한 이유가 여기 있다.

## 5.5 RQ5 — 안전성 (EXP-E)

### P1/P2 — hallucination

`halluc_energy`: 삭제한 구간에서 출력이 만들어낸 에너지 / 원래 에너지. **낮을수록 안전.**

| 방법 | P1 (1 beat 삭제) | P2 (asystole 3 s) |
|---|---|---|
| `M06` | 0.0329 | **0.0436** |
| `M08` | 0.0370 | 0.0463 |
| `M07` | 0.0435 | 0.0516 |
| `M04` | 0.0650 | 0.1097 |
| `M05` | 0.0799 | 0.0838 |
| `M_FE` | 0.1220 | 0.1591 |
| `M00` | 0.1618 | 0.4009 |

**딥러닝이 가장 안전하다.** 3초 심정지 구간에서 만들어내는 에너지가 가장 적다.
residual 구조(출력 = 입력 − 예측잡음)의 직접적 결과다. `M00`/`M_FE` 가 높은 것은
생성이 아니라 **잡음이 그대로 남아서**이므로, 이 지표는 EXP-A 와 함께 읽어야 한다.

### P3 — 부정맥(PVC) 훼손

`V − N`(부정맥 beat 와 정상 beat 의 template 상관 차). 크게 음수면 부정맥을 훼손한다.

| 방법 | N | V | V − N |
|---|---|---|---|
| `M08` | 0.9871 | 0.9940 | +0.0069 |
| `M06` | 0.9865 | 0.9939 | +0.0074 |
| `M04` | 0.9671 | 0.9867 | +0.0196 |
| **`M05`** | 0.9744 | 0.9099 | **−0.0645** |

**`M05`(Sameni EKS)만 부정맥을 훼손한다.** 정상 beat 로 만든 위상 템플릿이
PVC 를 정상 쪽으로 끌어당기기 때문이다. 이 방법은 이미 5.1 에서 `M01` 과
구분되지 않았고 5.3 에서 왜곡도 컸는데, 안전성에서도 유일한 위험 요소다.

## 5.6 하류 과제와 계산 비용

R-peak 검출 (전 SNR 평균):

| 방법 | `Se` | `PPV` | `rpeak_mae_ms` | RTF |
|---|---|---|---|---|
| `M00` | 0.9865 | 0.9769 | 8.69 | 0 |
| `M04` | 0.9870 | 0.9812 | 8.73 | 0.0012 |
| `M06` | 0.9873 | **0.9957** | 8.56 | 0.0016 |
| `M07` | **0.9888** | **0.9976** | 8.61 | 0.0027 |
| `M08` | 0.9854 | 0.9935 | 8.61 | 0.0019 |
| `M05` | 0.9864 | 0.9796 | 8.81 | **0.0310** |

**딥러닝은 `PPV` 를 크게 올린다** (0.977 → 0.996). 오검출을 줄인다는 뜻이고,
`rpeak_mae_ms` 만 봐서는 보이지 않는다(전 방법이 8.6~8.8 ms 로 검출기 산포 안에
묻힌다). `Se`/`PPV` 를 주 지표로 승격한 이유가 이것이다.

**`M05` 가 가장 느리다** — RTF 0.031 로 `M04` 의 26배, `M08` 의 16배다.
계산이 가장 비싸면서 왜곡이 가장 크고 부정맥을 훼손하는 유일한 방법이다.

모든 방법이 RTF < 0.04 로 **실시간의 25배 이상 빠르다.** 계산 비용은 이 과제에서
변별 요인이 아니다.

## 5.7 front-end 를 고치자 무엇이 바뀌었는가

초판(F-10 이전)과 현재를 같은 지표로 비교한다. 바뀐 것은 **딥러닝에도 동일한
front-end 를 준 것 하나**이고, 나머지 조건은 같다.

| 항목 | 초판 (DL 만 FE 없음) | 현재 (전 방법 FE) |
|---|---|---|
| 15 dB 최고 | `M04` 7.79 | **`M08` 8.71** |
| −5 dB 최고 | `M07` 18.54 | `M06` 17.25 |
| `M06` vs `M01` 통계 | **n.s.** (p=0.101) | **유의** (p=9e−6, r=0.99) |
| `M08` 왜곡 하한 | 23.19 dB | **27.59 dB** |
| `M06` 왜곡 하한 | 22.33 dB | **25.04 dB** |

**결론이 두 개 뒤집혔다.**

1. **"실측 조건에서는 튜닝된 DSP 가 딥러닝보다 낫다"** → 틀렸다. 15 dB 에서
   `M08` 이 `M04` 를 1.68 dB 앞서고 통계적으로 유의하다. 초판의 결론은 딥러닝이
   기저선 변동 제거를 처음부터 학습해야 하는 불리한 조건에서 나온 것이었다.
2. **"딥러닝은 `M01` 대비 유의하지 않다"** → 틀렸다. 이제 22개 record 전부에서
   이긴다(effect r = 0.99~1.00).

**살아남은 결론도 있다.**

- 20 dB 에서는 여전히 `M04` 가 앞선다. 잡음이 적으면 신경망의 변형이 이득을 넘는다.
- `M04` 는 clean 신호를 거의 훼손하지 않는다(front-end 대비 0.03 dB).
- `M05` 는 계산이 가장 비싸고 왜곡이 가장 크며 부정맥을 훼손한다.

**그리고 초판이 답할 수 없었던 질문에 답이 생겼다.** `M_FE` 를 필수 행으로 넣은
덕분에 "이 이득 중 얼마가 front-end 의 몫인가"를 모든 표에서 읽을 수 있게 됐고,
그 결과 5.2 의 (1)(2)(3)과 5.3 의 oracle 천장이 드러났다. 초판의 오독은 단순한
실수가 아니라 **기준선이 없어서 구조적으로 볼 수 없었던 것**이다.

## 5.8 D1 — 실데이터(MIT-BIH)에서 다시 물었다

5.1~5.7 은 전부 **합성축 D0** 의 결과다. 합성 데이터는 정답을 알 수 있어 평가가
정확하다는 장점이 있지만, 그 정확한 평가가 **실제를 예측하는가** 는 별개의
질문이다. F-8 에서 합성 벤치마크가 딥러닝을 12 dB 과대평가하고 있었음을 이미
겪었으므로, 이 질문은 이 과제에서 특히 무겁다.

D1 은 **MIT-BIH 실기록 + NSTDB 실잡음**이다. 같은 방법·같은 지표·같은 절차로
전부 다시 학습하고 다시 측정했다. 자동 생성 표는 `docs/90_results_d1.md`,
두 축 대조는 `docs/92_axis_gap.md`.

> **참조 정의가 다르다.** D1 의 MIT-BIH 원본은 clean 이 아니므로 참조를
> `FE(원본)` 으로 정의했다(F-12, D-3). 즉 D1 에서 묻는 것은 "원본 복원" 이
> 아니라 **"0.5~100 Hz 대역의 ECG 복원"** 이다. D0 에서는 두 정의의 차이가
> 0.02 dB 이하라 재실행하지 않았다(D-4).

### 5.8.1 전 구간 평균만 보면 "딥러닝이 실데이터에서 안 된다"

`M01`(고전 bandpass) 대비 `snr_imp_scaled`, paired Wilcoxon + Holm, 22 기록:

| | D0 Δ | D0 p(Holm) | D1 Δ | D1 p(Holm) |
|---|---|---|---|---|
| `M08` | +6.573 | 6.2e-06 | +1.629 | **0.181** (n.s.) |
| `M06` | +6.033 | 8.6e-06 | +1.338 | **0.492** (n.s.) |
| `M07` | +5.060 | 8.6e-06 | +0.126 | **1.000** (n.s.) |

D0 에서 22 기록 전부를 이기던(effect r ≈ 1.0) 딥러닝이 D1 에서는 Holm 보정 후
유의성을 잃는다. 여기서 멈추면 **"합성에서만 되는 방법"** 이 결론이 된다.

**그 결론은 틀렸다.** 전 구간 평균 하나로 물었기 때문에 나온 답이다.

### 5.8.2 구간을 나누면 다른 것이 보인다

기준선을 `M_FE`(공통 front-end, **참조와 같은 대역**)로 두고 입력 SNR 구간별로
`M08` 의 순수 기여를 본다. 이것이 "딥러닝이 front-end 위에 무엇을 더하는가" 를
재는 올바른 비교다.

| 입력 SNR | D0 Δ | D0 판정 | D1 Δ | D1 판정 |
|---|---|---|---|---|
| **−5 dB** | +7.895 | 유의 | **+7.072** | **유의** |
| **0 dB** | +8.373 | 유의 | **+6.656** | **유의** |
| **5 dB** | +7.464 | 유의 | **+4.813** | **유의** |
| 10 dB | +5.726 | 유의 | +2.128 | n.s. |
| 15 dB | +3.625 | 유의 | −0.914 | n.s. |
| 20 dB | +1.258 | n.s. | −4.452 | n.s. |

**가장 어려운 −5 dB 조건에서 두 축이 거의 같다 (+7.9 vs +7.1).**
딥러닝이 실데이터에서 약해진 것이 아니다.

달라진 것은 **딥러닝이 도움이 되는 SNR 범위**다. 이득이 0 을 지나는 지점을
표의 인접 두 점으로 선형 보간하면 D1 은 **13 dB 부근**이고, D0 는 20 dB 에서도
아직 +1.26 dB 라 **측정 범위(20 dB) 밖**이다. 그 위에서는 공통 front-end 만
쓰는 편이 낫다. `[추론]` (보간이므로 정확한 교차점이 아니라 자릿수다.)

전 구간 평균은 이 여섯 구간에 같은 무게를 준다. 저 SNR 의 +7 dB 와 고 SNR 의
−4 dB 가 상쇄되어 n.s. 가 된 것이다.

**실용적 함의가 바뀐다.** "딥러닝을 쓸 것인가" 가 아니라 **"입력 SNR 이
13 dB 아래인가"** 가 질문이다. D0 만 봤다면 이 질문 자체가 생기지 않았다.

> **이 분기점을 재는 것 자체가 문제였다.** 실측 장비가 어느 구간인지는
> ground-truth 없는 SNR 추정기로 재야 하는데, 그 추정기의 **천장이 11.9~12.8 dB**
> 다 — 판단해야 할 지점이 정확히 측정 수단의 천장에 놓여 있다(F-18).
> 잡음을 하나도 넣지 않은 실기록에서도 추정치가 12.9 dB 로 나온다.
>
> 해결책은 추정기 선택이었다. **등전위(TP) 구간 기반 추정기 (C)는 천장이
> 15.6 dB** 로 13 dB 위에 여유가 있고, beat 형태 변동에 흔들리지 않는다.
> P-10 의 실측 절차를 (C) 중심으로 정했다 (7.3).

### 5.8.3 SNR 밖에서는 딥러닝 우위가 그대로 유지된다

`M_FE` 대비 D1 전 구간:

| metric | Δ | p(Holm) | effect r | 뜻 |
|---|---|---|---|---|
| `gain_bias` (→1) | +0.165 | 6.2e-06 | **+1.000** | 진폭 보존 — **22 기록 전부** |
| `ppv` | +0.021 | 2.6e-03 | **+1.000** | R-peak 오검출 감소 — **22 기록 전부** |
| `cc` | +0.070 | 6.3e-05 | +0.945 | 파형 상관 |
| `prdn` | −22.9 | 3.8e-05 | −0.960 | 정규화 오차 |

`M_FE` 는 진폭을 15 % 깎는다(gain_bias 0.848). 딥러닝은 1.013 으로 보존한다.
**`snr_imp_scaled` 는 이득 보정(α\*)을 하므로 이 차이를 원리적으로 보지 못한다.**

> 다만 이것이 5.8.1 의 n.s. 를 설명하지는 않는다. 이득 보정을 하지 않는
> `snr_imp_strict` 도 전 구간 평균에서 n.s. 다(`M08` +1.98, p=0.109).
> 5.8.1 의 원인은 5.8.2 의 구간별 상쇄이고, 이득 보정은 **`gain_bias` 의
> 우위가 SNR 표에 나타나지 않는 이유**일 뿐이다.

`ppv` 는 하류 과제(R-peak 검출) 지표다. 22 기록 전부에서 딥러닝이 오검출이
적다 — 진단 파이프라인 관점에서는 SNR 보다 이쪽이 중요하다.

### 5.8.4 안전성 — 딥러닝이 가장 안전하다

EXP-E(안전성 프로브)는 "없는 파형을 만들어내는가" 를 잰다. beat 를 지우고
잡음을 덮은 뒤, 출력이 그 자리에 만든 에너지를 원래 에너지로 나눈 값이다.
**낮을수록 안전**하다.

| 방법 | P1 beat dropout | P2 asystole 3 s |
|---|---|---|
| `M07` | **0.0113** | **0.0311** |
| `M06` | 0.0137 | 0.0371 |
| `M08` | 0.0158 | 0.0314 |
| `M05` | 0.0366 | 0.0720 |
| `M01` | 0.0437 | 0.0976 |
| `M_FE` | 0.0543 | 0.1247 |

딥러닝 세 방법이 `M01` 대비 **2.8~3.9 배**, `M_FE` 대비 **3.4~4.8 배**
낮다. residual 구조
(출력 = 입력 − 예측잡음)의 직접적 결과다 — 모델이 잡음을 예측할 뿐 신호를
그리지 않으므로, 신호가 없는 자리에 뺄 것도 없다.

부정맥 편향도 없다: P3 의 `V − N`(부정맥 beat 형태 보존 − 정상 beat 형태 보존)이
전 방법에서 양수다.

**이것이 의료 신호 처리에서 딥러닝에 대한 통상적 우려와 반대되는 결과**라는
점을 강조해 둔다. "딥러닝은 그럴듯한 파형을 지어낸다" 는 걱정은 **생성형
구조**에 해당하는 것이고, residual 구조에는 해당하지 않는다.

### 5.8.5 D0 의 결론 중 무엇이 살아남았나

| D0 결론 | D1 |
|---|---|
| 저 SNR 에서 딥러닝이 front-end 를 크게 앞선다 | **재현** (−5 dB 에서 +7.07 dB, p=1.7e-05) |
| 임펄스 잡음에서 딥러닝이 oracle 을 넘는다 | **재현** (`M08` 13.69 vs `B01` 11.81, `B02` 6.93) |
| wavelet thresholding 이 bw 에서 기여가 없다 | **재현** (bw: `M04` 23.92 < `M_FE` 24.70) |
| `M05`(Sameni)가 `M01` 과 구분되지 않는다 | **재현**, 그리고 D1 에서는 QRS 를 훼손 (F-14) |
| 고 SNR 에서도 딥러닝이 앞선다 | **재현 안 됨** — 12 dB 부근에서 역전 |
| `M08` > `M06` | **재현 안 됨** — D0 +0.540 dB(p=0.012) → D1 +0.292 dB(p=0.70) |
| `M05` 가 PVC 를 훼손하는 유일한 방법 | **재현 안 됨** — `V − N` 이 D0 −0.065 → D1 **+0.014**(전 방법 양수) |
| `rpeak_mae_ms` 는 판별력이 없다 | **뒤집힘** — D1 에서는 `M08`·`B01` 이 `M01` 대비 유의 |

**잡음 종류별 강약(RQ2)의 구조가 그대로 재현된 것**이 가장 든든하다.
어떤 잡음에 어떤 계열이 강한가는 물리적 이유가 있는 결론이고, 그것이 축을
건너 살아남았다.

`M05` 의 PVC 훼손이 사라진 것은 F-14 와 같은 방향으로 읽힌다 `[추론]`.
D0 는 McSharry ODE 로 생성되므로 **정상 beat 가 모형에 정확히 맞고 PVC 만
모형을 위반**한다. 칼만 필터는 모형을 위반하는 성분을 잡음으로 보고 지우므로
PVC 가 선택적으로 훼손된다. D1 에서는 정상 beat 도 모형을 벗어나므로 PVC 가
특별히 불리하지 않다. **즉 D0 에서 관측된 "PVC 훼손" 은 방법의 임상적 위험이
아니라 합성 데이터의 성질이었을 수 있다.** 단정하지 않는 이유는 F-17 과 같다 —
D0→D1 은 생성 모형만 바뀌는 것이 아니다.

### 5.8.6 해석에서 쓰지 않기로 한 것

결과를 읽으면서 **쓸 뻔했다가 검증에서 걸러낸 것 두 가지**를 남긴다.
둘 다 표에 유의한 숫자로 나타나 있어서 그대로 인용하기 쉬웠다.

**(a) `psd_logdist` 에서 딥러닝이 `M01` 을 18 dB 앞선다 — 쓰지 않는다.**
22 기록 전부에서 유의하다(p=6.2e-06, r=−1.000). 그런데 `M01` 은 0.5–40 Hz
bandpass 이고 **참조는 0.5–100 Hz** 다. 40~100 Hz 에 `M01` 은 아무것도 남기지
않는데 참조에는 내용이 있으니, 그 빈 대역에서 log-PSD 거리가 폭발한다.
같은 대역인 `M_FE` 와 비교하면 **두 축 모두 유의하지 않다**(D0 p=0.13,
D1 p=0.68). 잡음 제거 성능이 아니라 절단 주파수의 차이였다.

**(b) `rmse`/`prdn` 의 우세를 SNR 과 독립된 증거로 쓰지 않는다.**
D1 전 구간에서 `rmse` 는 유의한데(p=2.4e-05) `snr_imp_scaled` 는 아니다.
서로 다른 것을 재는 것처럼 보이지만 **구간별로는 완전히 일치한다.** 차이는
평균 방식이다 — D1 −5 dB 의 rmse(0.220)가 20 dB(0.019)의 **12 배**라, 전 구간
선형 평균은 사실상 저 SNR 성능이다. 같은 사실을 두 번 세는 셈이 된다.

> **두 사례의 공통점**: 통계적으로 유의하고 effect size 도 큰데, **비교의 전제가
> 달랐다.** 이 프로젝트에서 반복된 패턴이다(F-8·F-10·F-11·F-12·F-15·F-16·F-17).
> p 값은 전제가 틀렸다는 것을 알려주지 않는다.


### 5.8.7 손실 선택이 고 SNR 열세를 일부 되돌린다 — 그러나 되돌리지 않았다

주 실험(5.8.1~5.8.6)의 딥러닝 세 방법은 전부 **L1 손실**로 학습한 체크포인트다.
loss ablation(`docs/10_loss_ablation_d1.md`)은 D1 에서 그 선택이 최선이 아니라고
말한다. TEST split, `M08-L1` 기준 `[측정]`:

| 비교 | 전 구간 Δ | p(Holm) | effect r | 0 dB | 10 dB | 20 dB |
|---|---|---|---|---|---|---|
| `M08-L3` | +1.032 | 1.1e-03 | +0.842 | +0.54 | +0.78 | **+1.78** |
| `M08-L4` | +0.993 | 1.3e-02 | +0.708 | −0.40 | +0.91 | **+2.47** |

**이득이 고 SNR 에 집중된다** — 5.8.2 에서 딥러닝이 지고 있던 바로 그 구간이다.
20 dB 에서 `M08` 은 `M_FE` 에 −4.45 dB 뒤지는데, L3 를 쓰면 그 격차가 −2.7 dB
수준으로 줄고 교차점이 13 dB 에서 **15~16 dB 부근**으로 올라간다 `[추론]`.

D0 에서는 양상이 다르다. 거기서는 `M06` 이 L3 로 이득을 보고(+1.131, p=1e-04)
`M08` 은 유의한 차이가 없다(+0.142, p=0.26). **어느 손실이 유리한가가 축에
따라 다르다**는 것 자체가 결과다.

**그런데 주 실험을 L3 로 다시 돌리지 않았다.** 위 수치는 TEST split 에서 나온
것이고, 그것을 보고 주 실험의 설정을 고르면 **사후 선택**이 된다. TEST 를
모델 선택에 쓰는 순간 그 TEST 는 더 이상 독립 평가가 아니다 — 이 프로젝트가
F-8 에서 겪은 leakage 와 종류만 다른 같은 문제다.

따라서 이 결과는 **주 표를 고치는 근거가 아니라 주 표의 범위를 한정하는
근거**로 쓴다: 5.8 의 결론은 "딥러닝이 고 SNR 에서 못한다" 가 아니라
**"L1 로 학습한 딥러닝이 고 SNR 에서 못하고, 손실을 바꾸면 그 격차의 40 %
정도가 줄어든다"** 이다. 손실을 사전에 고정한 채 다시 재는 것은 다음 단계다.

## 5.9 RQ7 — 시간 해상도를 유지하면 QRS 가 살아나는가 (M10)

### 5.9.1 질문과 판의 설계

딥러닝 세 방법(`M06`·`M07`·`M08`)은 **전부 U-Net 계열**이라 신호를 1/2 씩 4회
줄였다 복원한다. **QRS 는 폭 80~100 ms(250 Hz 에서 20~25 샘플)의 가장 빠른
성분**이므로 1/16 지점에서는 1~2 샘플이 된다. 그 병목이 QRS 를 뭉개는가.

이 질문만 남기려고 `M10` DilatedResNet1D 를 만들었다 — **downsampling 없이
dilation(1·2·4·8·16·24)으로만 수용영역을 넓히는** 구조다. `M06` 과 params 를
0.4 %, 수용영역을 0.9 % 안에서 맞추고 손실·데이터·분할·일정·시드를 공유했다.
**차이는 시간 해상도 하나다.** 설계 근거와 대안 검토는 D-11 에 있다.

> **TCN 과의 관계.** 이 구조는 TCN 계열에서 dilation·해상도 유지만 가져오고
> **causal masking 은 일부러 뺐다.** 우리는 오프라인 후처리이고 다른 방법이
> 전부 비인과적이라, causal 로 묶으면 해상도가 아니라 인과성을 재게 된다.
> 따라서 이 절의 결과를 **"TCN 을 시험했다" 로 읽으면 안 된다.**

### 5.9.2 결과 — 핵심 가설 기각, 그리고 형태 손상

D0·D1 `exp_a` TEST, `M10 − M06`, paired Wilcoxon + Holm, 기록 단위 n=22
`[측정]`. `floor` 는 각 축의 `floor_p95` 이고 **`|Δ|/floor < 1` 이면 p 값과
무관하게 구분 불가**다(F-16, O-14).

| 지표 | 축 | Δ (M10−M06) | \|Δ\|/floor | p(Holm) | 판정 |
|---|---|---|---|---|---|
| **`qrs_dur_err_ms`** | D0 | −1.834 | 2.9 | 0.897 | **n.s.** |
| | D1 | −0.072 | 0.0 | 0.992 | 구분 불가 |
| **`beat_cc`** | D0 | **−0.00552** | 15203 | **0.023** | **M10 열세** |
| | D1 | **−0.00190** | 4683 | **0.029** | **M10 열세** |
| `gain_bias` (이상 1) | D0 | +0.0463 | 227 | 6.7e-06 | M10 열세 |
| | D1 | +0.0195 | 101 | 4.0e-05 | M10 열세 |
| `snr_imp_scaled` | D0 | −0.912 | — | 0.021 | M10 열세 |
| | D1 | −0.591 | — | 0.211 | n.s. |
| `psd_logdist` | D0 | +0.320 | 0.1 | 0.181 | 구분 불가 |
| | D1 | −0.731 | 0.7 | 0.012 | 구분 불가 |
| `rmse` | D0 / D1 | +0.001 / +0.003 | 0.7 / 0.7 | 1 / 0.217 | 구분 불가 |

**(1) 해상도를 유지해도 QRS 폭 오차는 나아지지 않는다.** D0 는 이 지표가
실제로 답할 수 있는 축이다 — floor 가 0.640 ms 이고 관측된 차이는 그 2.9 배다.
그런데도 `p = 0.897` 이다. D1 은 floor 가 28.07 ms 라 애초에 못 가른다.

**(2) 오히려 beat 형태가 나빠졌다 — 양축에서 거의 같은 크기로.**
`beat_cc` 는 D0 −0.00552(r = −0.723), D1 −0.00190(r = −0.715)로 **효과 크기가
두 축에서 사실상 같다.** floor 의 4,700~15,000 배라 분해능 문제가 아니다.
`beat_cc` 는 상관이라 진폭 배율에 불변이므로 (3)과 **독립된 손상**이다.

**(3) `M10` 은 신호를 체계적으로 2~5 % 눌러 내보낸다.** `gain_bias` 는 1 보다
크면 출력이 참값보다 작다는 뜻이다 — `M10` 1.027 / 1.029 vs `M06` 0.980 / 1.010.
D0 에서 rank-biserial **r = +1.000, 22 기록 전부**가 같은 방향이다.

### 5.9.3 대가

| | `M06` | `M10` | 배수 |
|---|---|---|---|
| params | 976,489 | 979,926 | 1.00 |
| 연산량 (이론 MAC/window) | 95.9 M | **999.6 M** | **10.4** |
| 추론 (CPU 1 thread, 1024 window) | 11.1 ms | **33.6 ms** | **3.0** |

downsampling 이 없으니 모든 블록이 전 길이에서 돈다. **이 프로젝트는 임베디드
실시간을 향하고 있어 연산량 10 배는 결정적이다** — 성능이 같기만 해도 탈락인데
형태 지표가 나쁘다.

### 5.9.4 판정을 이 두 지표에만 얹은 이유

`M10` 은 D0 에서 **18 epoch 만에 멈췄고 최고 기록이 6 번째 epoch** 이었다.
같은 조건의 `M06` 은 37 epoch 을 돌았다. 즉 **D0 의 학습량 자체가 불공평**하고,
D0 의 `snr_imp_scaled` 열세(−0.912)를 구조 탓으로 돌릴 수 없다.

D1 은 반대다. `M10` 이 **더 오래(52 vs 46 epoch)** 돌았고 수렴 VAL 중앙값도
`M10` 이 더 높다(+2.985 vs +2.645). **그럼에도 TEST 의 `beat_cc`·`gain_bias`
열세는 양축에서 그대로다.** 학습량으로 설명되지 않는 것은 이 둘뿐이고, 그래서
결론을 이 둘에만 얹었다. (조기 종료가 런마다 다르게 작동하는 문제 자체는
F-19 에서 따로 다룬다.)

### 5.9.5 잡음 종류를 가려도 유리한 자리가 없다

"어떤 잡음에는 해상도 유지가 유리하지 않겠는가" 가 남은 반론이다. EXP-B
(잡음 7종, **입력 SNR 10 dB 고정**, 22 기록)로 갈라 봤다. `snr_imp_scaled`,
`M10 − M06`, paired Wilcoxon + Holm `[측정]`:

| 잡음 | D0 Δ | D0 p | D1 Δ | D1 p |
|---|---|---|---|---|
| `awgn` 백색 | −1.155 | 6.7e-06 | −0.972 | 0.010 |
| `bw_synth` 기저선 변동 | −1.930 | 4.5e-05 | −1.194 | 0.086 |
| `em_synth` 전극 움직임 | −1.391 | 3.0e-05 | **+0.196** | 1 |
| `impulse` 임펄스 | −1.456 | 2.7e-05 | −1.046 | 0.011 |
| `ma_synth` 근전도 | −1.863 | 6.7e-06 | −1.073 | 0.004 |
| `mixed` 혼합 | −1.120 | 0.001 | −0.756 | 0.516 |
| `pli` 전원선 | −2.100 | 3.3e-05 | −1.523 | 0.042 |

**D0 는 7종 전부 M10 열세이고 전부 유의하다** — `awgn` 과 `ma_synth` 는
rank-biserial **r = −1.000, 22 기록 전부**다. D1 은 4종이 유의하게 열세이고
나머지 3종은 n.s. 다.

**어느 축, 어느 잡음에서도 M10 이 유의하게 앞선 자리는 없다.** 부호가 한 번
뒤집히는 곳이 D1 의 `em_synth`(+0.196)인데 `p = 1` 이라 근거가 되지 않는다.
전극 움직임은 스펙트럼이 ECG 와 가장 많이 겹쳐 가장 어려운 잡음인데, 하필
거기서만 부호가 뒤집히는 것은 **차이가 사라지는 지점**으로 읽는 것이 맞다.

(D0 의 유의성이 D1 보다 일관되게 강한 것은 5.9.4 의 학습량 불균형과 같은
방향이므로, **D0 만으로 강한 주장을 하지 않는다.** 결론은 양축 공통 항목에
얹었다.)

### 5.9.6 세 번 연속 기각 — 그래서 어디에 자원을 넣나

이것으로 "구조를 바꾸면 나아진다" 가 세 번 기각됐다.

| 시도 | 무엇을 바꿨나 | 결과 |
|---|---|---|
| `M07` | SWT 를 앞에 (RQ4) | `M06` 대비 열세 |
| `M08` | wavelet subband 로 분해 (RQ4) | `M06` 대비 n.s. |
| `M10` | 시간 해상도 유지 (RQ7) | **n.s. + 형태 열세 + 연산 10 배** |

반면 **손실 변경은 유의했다** — 다만 어느 모델이 반응하는지가 축마다 다르다.

| | D0 | D1 |
|---|---|---|
| `M06` L1→L3 | **+1.131 (p = 1.0e-04)** | +0.869 (p = 0.051) |
| `M08` L1→L3 | +0.142 (p = 0.258) | **+1.032 (p = 1.1e-03)** |

**양축 어디서도 유의한 구조 변경은 없었고, 양축 모두 유의한 손실 변경이
하나씩 있다.** 다음 자원은 구조가 아니라 손실에 넣는 것이 맞다.

# 6. 개발 과정에서 발견한 것

결과보다 이쪽이 방법론적으로 더 값질 수 있다. 전부 실측 근거와 회귀 테스트가 있다.
번호 역순(늦게 발견한 것부터)으로 싣는다. **여기 실린 것은 요약이고, 전문은
`docs/20_findings.md` 에 있다** — 각 항목의 발단(무엇이 이상해 보였나), 먼저
의심했다가 배제한 가설, 결정적 측정, 놓쳤다면 무엇이 실렸을지가 기록돼 있다.
결과를 안 바꾼 운영 사고는 `docs/22_incidents.md`, 설계 결정은 `docs/21_decisions.md`.

**F-17 · F-16 · F-15 · F-12 · F-11 · F-10 · F-8 은 성격이 같다: 방법이 아니라
측정 틀이 틀렸던 경우다.** 일곱 다 "그럴듯한 결과표" 를 만들어냈고, 표만
봐서는 드러나지 않았다. 8장 끝의 방법론적 요약에 이 계열을 따로 정리했다.

## F-20. 시간 해상도를 유지해도 QRS 는 나아지지 않는다 — beat 형태는 나빠진다

`M06` 과 params·수용영역을 1 % 안에서 맞추고 **downsampling 만 없앤** `M10` 을
만들어 물었다. QRS 폭 오차는 양축에서 나아지지 않았고(D0 p=0.897), `beat_cc` 는
**양축에서 나빠졌다**(r = −0.723 / −0.715 — 효과 크기가 사실상 같다). 연산량은
10.4 배다. **잡음 7종으로 갈라 봐도 M10 이 유의하게 앞선 자리는 없다.**
전문과 결과는 5.9 절.

**D1 만 봤으면 반대로 실었다.** D1 의 `psd_logdist` 는 −0.731, `p = 0.012` 로
`M10` 의 유일한 우세 항목이었는데, (a) D1 floor 가 0.998 이라 **분해능 아래**고
(b) D0 에서는 **부호가 반대**(+0.320)다. 축 하나와 유의성 하나만 보면 통과했을
서술이다 — 실제로 인수인계 문서에 그렇게 적혀 있었다(O-14). 이후 비교표는
`|Δ|/floor` 열 없이 만들지 않는다. `ecgdn/eval/stats.py::compare_methods` 가
`floor` 인자를 받아 `resolvable` 을 계산한다.

## F-19. VAL 이 수렴 후에도 튀어서 '최고 epoch' 선택이 런마다 다르게 부풀려진다

`M10` 을 `M06` 과 대조하다가, `M10` 이 **6 epoch 에 최고를 찍고 18 epoch 에
멈춘** 반면 `M06` 은 37 epoch 을 돈 것이 눈에 띄었다. 전 런의 학습 궤적을 열어
수렴 구간의 산포를 재니, **`best` 는 수렴 중앙값보다 평균 +0.88 dB 부풀려져
있고 그 크기가 런마다 다르다** — 부풀림이 그 런의 VAL 잡음에 비례한다
(+0.380 ~ +1.486 dB). 즉 **`best` 끼리 비교하면 잡음이 큰 런이 유리하다.**

영향이 둘이다. 체크포인트 선택이 운에 좌우되고, **조기 종료가 운 좋은 epoch
에서 발동해 학습량 자체를 불공평하게 만든다.** 그래서 5.9 의 판정은 VAL 이
아니라 TEST 로, 그것도 **학습량으로 설명되지 않는 지표에만** 얹었다.

## F-18. 추정기의 천장이 축마다 8 dB 다른데 포화 경고는 상수 하나였다

잡음을 **하나도 넣지 않은** 실기록에 SNR 추정기를 돌리니 12.9 dB 가 나왔다.
잡음이 없으면 무한대여야 하므로, 그 유한한 값이 **추정기의 천장**이다 —
beat 마다 형태가 다른 것을 잡음으로 세고 있다.

| 추정기 | D0 | D1 | 차이 |
|---|---|---|---|
| (A) beat 평균 잔차 | 19.6 | **11.9** | −7.7 |
| (B) half-sample | 19.8 | **12.8** | −7.0 |
| (C) 등전위 구간 | 16.8 | 15.6 | −1.2 |

이 8 dB 가 F-13 의 −7.6 dB 편향과 일치한다 — 원인이 맞았고 크기까지 설명된다.

그런데 포화 경고 상수가 `22.0` 이었다. **D0 의 천장(19.6)조차 넘는 값이라
어느 축에서도 뜨지 않는 장치**였다. 추정치가 이미 천장인데 경고가 조용했다.

> **놓쳤다면**: 장비 SNR 을 재고 "12 dB 니 딥러닝을 쓰는 구간" 이라고
> 결론지었을 것이다. 그 12 dB 는 장비의 SNR 이 아니라 추정기의 최대치이고,
> 장비가 실제로 18 dB 여도 같은 값이 나온다.
>
> **일반화**: **경고 장치의 임계값도 측정해야 한다.** 코드가 정상이고 값도
> 정상인데 **한 번도 뜨지 않아서** 아무 일도 하지 않고 있었다. 테스트는
> "뜨는 조건에서 뜨는가" 를 확인하지 "실제 데이터에서 뜬 적이 있는가" 를
> 확인하지 않는다. 진단 질문: **이 경고가 마지막으로 뜬 것이 언제인가.**

## F-17. Sameni 자가진단의 "참 파라미터" 가 실데이터에서는 참이 아니었다

주입하던 `DEFAULT_KERNEL` 은 **합성 신호를 만드는 바로 그 커널**이다. D0 에서는
정답 주입이지만 D1 에서는 그 환자와 무관한 커널이다 — 실제 ECG 에 대응하는
McSharry 참 파라미터는 존재하지 않는다.

**단서는 숫자 자체였다**: D1 에서 적합 커널(+11.23 dB)이 주입 커널(+5.91 dB)보다
좋았다. 정의상 천장이어야 할 값이 천장이 아니었다.

이 때문에 F-14 의 초판 근거표(`DoD: D0 +16.2 vs D1 +5.91`)를 철회하고 EXP-A 로
다시 세웠다. DoD 는 합성축 전용 검사로 선언했다.

> **놓쳤다면**: "실데이터에서는 참 파라미터를 줘도 5.91 dB 뿐 — 모형 가정이
> 무너진다" 가 실렸을 것이다. 문장은 그럴듯하고 숫자도 실재하지만, 그 값은
> 참 파라미터를 준 값이 아니다.

## F-16. 지표의 분해능이 데이터축에 따라 44 배 달라진다

`qrs_dur_err_ms` 의 floor 가 D0 0.640 ms, **D1 28.07 ms** 다. 합성 ECG 는 beat
마다 파형이 거의 같아 delineator 가 안정적인데, 실기록은 호흡·축 변화·부정맥으로
경계 판정이 크게 흔들린다.

결과적으로 **D1 에서 QRS duration 은 방법을 가르지 못한다** — loss 간 폭이
3.7~13 ms 로 분해능에 완전히 묻힌다. ablation 표에서 '최선' 굵은 표시를 걷었다.

> **놓쳤다면**: "D1 에서는 L2/L3 가 L1 보다 QRS 를 잘 보존한다"(25.995 → 22.267 ms)
> 가 결과로 실렸을 것이다.

## F-15. D1 에서 `M_FE` 의 왜곡 하한 147 dB 는 성능이 아니라 참조 정의의 항등식

D-3 으로 D1 의 참조를 `FE(원본)` 으로 정의했으므로, `M_FE` 의 왜곡 하한은
`SNR(FE(x), FE(x)) = ∞` 다. 147 dB 는 그 무한대가 float64 에서 잘린 값이다.

front-end 를 쓰지 않는 `M06`/`M08` 은 두 축에서 비슷한데(24.9→22.2, 27.4→23.5)
front-end 계열만 D1 에서 크게 올랐다(`M_FE` 34.1→147.3, `B01` 34.1→71.6).

> **놓쳤다면**: Pareto 그림에서 `M_FE` 가 왜곡 축 맨 오른쪽에 찍혀
> **"front-end 는 신호를 전혀 손상시키지 않는다"** 가 결론이 됐을 것이다.
>
> **일반화**: 참조 정의를 바꾸면 그 정의에 포함된 연산을 쓰는 방법이 공짜
> 점수를 받는다. 재정의할 때 "누가 이득을 보는가" 를 먼저 따져야 한다.

## F-14. Sameni 는 커널을 아는 조건에서만 강하다

커널을 주입하면 D0 에서 +16.21 dB 로 매우 강하다. 그러나 커널을 추정해야 하는
실제 파이프라인에서는 **두 축 모두 단순 bandpass 를 이기지 못한다** — D0 무승부
(p=0.83), D1 은 2 dB 열세. 그리고 D1 에서 QRS 폭을 +30.4 ms 훼손한다
(p=4e-04, r=+0.905). 무처리(`M00`, 35.50 ms)보다도 나쁜 50.72 ms 다.

> **일반화**: 부가 정보를 받은 조건의 성능을, 그 정보를 추정해야 하는 조건의
> 성능처럼 인용하지 않는다. 그 격차 자체가 결과다 — 여기서는 +16.2 vs −2.0 dB.

## F-13. ground-truth 없는 SNR 추정기가 실데이터에서 8 배 편향된다

합성에서 교정한 추정기를 MIT-BIH 에 적용하면 편향이 8 배로 커진다.
**이것이 5.8.2 의 실용적 결론을 막고 있다** — 딥러닝 사용 여부의 분기점을
13 dB 로 정했는데, 실측 장비가 그 위인지 아래인지 잴 수단이 없다.

## F-12. MIT-BIH 를 '정답' 으로 두면 front-end 를 쓰는 모든 방법이 부당하게 진다

MIT-BIH 원본은 clean 이 아니다. 원본을 참조로 두면 front-end 가 제거한 성분이
전부 '오차' 로 계산되어, **front-end 를 통과하는 모든 방법이 구조적으로 진다.**
D0 에서 front-end 의 SNR 이 53.24 dB 인데 D1 에서 9.32 dB 였던 것이 그 크기다.

SWT 튜닝이 최선에서 **−0.49 dB** 를 내고 있었다. 파라미터가 나쁜 줄 알았는데
참조가 틀린 것이었다 — 고치자 **+12.47 dB** 가 됐다.

> **놓쳤다면**: D1 실험 전체가 무의미했을 것이다. 이 시점에 D1 학습을
> 시작했다면 잘못된 목표로 7 종을 학습했을 것이다.

## F-11. oracle 도 front-end 의 천장에 걸려 있다

EXP-C(왜곡 하한)에서 `B01`(oracle wavelet), `B02`(oracle Wiener), `M_FE`
(front-end 단독)가 **소수점까지 같은 34.13 ± 3.72 dB** 로 나왔다. oracle 은
정답을 알고 최적 복원을 하는데도 여기서 멈춘다 — **oracle 역시 front-end 를
통과하기 때문**이다.

같은 실행에서 두 번째 사실도 나왔다. wavelet thresholding 은 bw·em·impulse 에서
**구조적으로 0** 을 기여한다. `M04` 뿐 아니라 oracle `B01` 조차 그렇다
(bw −0.00, em +0.07). 기저선 변동은 근사계수 `A5` 에 실려 있고 thresholding 은
세부계수만 건드리므로, **정답을 알아도 못 고친다.**

> **일반화된 교훈**: oracle 을 "이론적 상한"으로 인용하기 전에 그것이 어떤
> 전처리를 포함하는지 확인해야 한다. 전처리를 공유하면 oracle 은 **그 전처리의
> 상한**이지 문제의 상한이 아니다. 그리고 oracle 이 못 고치는 조건은 그 계열
> 전체가 못 고치는 조건이며, 그 사실 자체가 결과다.

## F-10. "모든 방법에 동일 조건"이 선언에 그쳐 있었다

공정성 규약 (3)은 **모든 방법에 동일한 front-end를 준다**였다. 그런데 고전 기법은
전부 내부에서 front-end를 적용하는 반면, **딥러닝만 학습·추론 양쪽에서 raw를 받고
있었다.** 규약을 문서에 써 두고 코드에서 확인하지 않은 결과다.

입력 10 dB에서 실측:

| 잡음 종류 | `M_FE` front-end 단독 | `M04` FE+SWT | `M06` FE 없이 학습 |
|---|---|---|---|
| baseline wander | **20.26** | 20.26 | 10.58 |
| PLI 60 Hz | 17.46 | 19.41 | 10.58 |
| AWGN | 1.46 | **7.91** | 10.58 |
| impulse | 0.44 | 0.44 | 10.58 |

두 가지가 한꺼번에 드러난다.

1. **딥러닝이 진 것처럼 보이던 조건의 상당 부분이 front-end 유무였다.** 방법의
   우열이 아니라 실험 설계의 결함이다.
2. **bw/pli에서 SWT의 기여분은 정확히 0 dB다** (`M_FE` 20.26 = `M04` 20.26).
   이 문서의 초판은 "SWT가 baseline wander/PLI 조건에서 oracle에 근접한다"고
   썼는데, 그 이득은 SWT가 아니라 front-end가 낸 것이었다. **철회한다.**
   SWT가 실제로 일하는 곳은 AWGN(1.46 → 7.91)뿐이다.

> **일반화된 교훈**: 공정성 규약은 **선언이 아니라 측정으로** 확인해야 한다.
> 그래서 이제 모든 결과표에 `M_FE`(front-end 단독)를 필수 열로 넣는다.
> 각 방법의 이득 중 얼마가 공통 전처리에서 온 것인지 표 안에서 바로 읽히지 않으면,
> 같은 착각이 반복된다.

## F-9. 앞단을 고치면 뒤에 학습된 모든 체크포인트가 동시에 무효가 된다

F-10을 고친 뒤 `L1` 체크포인트만 재학습된 상태에서 loss ablation 표를 만들면
`L1(FE 있음)` vs `L3/L4(FE 없음)`을 비교하게 된다. 이 차이는 손실 함수 효과가
아니라 front-end 효과다. 게다가 그 표의 근거는 학습 로그의 **VAL** `snr_imp`였다 —
early stopping이 best를 고른 바로 그 집합이므로 낙관적으로 편향된다.

조치: 해당 STEP의 완료 표시를 되돌리고 결론을 철회했다. 재발 방지로 ablation 표
생성기가 체크포인트의 `frontend` 플래그와 **학습 시점 git hash**를 표에 함께 싣고,
설정이 섞여 있으면 경고 후 종료코드 2로 비교를 거부한다.

> **일반화된 교훈**: ablation 표는 "각 행이 언제 학습됐는가"를 기록하지 않으면
> 파이프라인 수정 이후 **조용히 거짓**이 된다. 표는 숫자만이 아니라 그 숫자가
> 만들어진 조건을 함께 실어야 한다.

## F-8. 합성 벤치마크가 딥러닝을 12 dB 과대평가하고 있었다

초기 합성 데이터 생성기는 **모든 기록에 같은 커널**을 썼다.
record 단위로 split을 해도 **morphology는 train/test가 완전히 동일**하므로,
1 M 파라미터 신경망이 생성기를 통째로 외운다.

| 조건 (입력 5 dB) | `M06` U-Net | `M04` SWT |
|---|---|---|
| 학습에 쓰인 morphology | **+16.5 dB** | +7.2 dB |
| 커널을 교란한 morphology | **+4~6 dB** | +8~12 dB |

수정 전 EXP-A 예비 실행에서 **DL +26 dB vs DSP +3 dB**가 나왔다.
이는 방법의 우열이 아니라 **벤치마크의 인공물**이었다.

> **일반화된 교훈**: split 축(record)과 **변이 축(morphology)이 일치하지 않으면
> record split은 leakage를 막지 못한다.**
> MIT-BIH에서는 기록마다 실제로 다른 환자이므로 이 문제가 없다 — 합성 데이터 고유의 함정이다.

조치: 기록(=피험자)마다 파형 위치·진폭·폭을 흔들고 낮은 확률로 T파를 역전시켰다.
기록 간 beat template 상관이 **1.0 → 0.0~0.6**으로 떨어졌고, 회귀 테스트로 고정했다.

## F-6. Sameni 구현 버그 2건 — "방법이 안 된다"가 아니라 "구현이 틀렸다"

| 버그 | 내용 |
|---|---|
| (a) | phase-averaged template의 **상수 오프셋**. 상태방정식은 `dz/dθ`만 규정하므로 오프셋은 동역학과 무관한데, 이를 방치하면 적합 R²가 0.99 → 0.82로 떨어지고 그 잔차가 `q_z` 과대추정으로 전파된다 |
| (b) | **`q_z`를 누적 분산으로 잘못 계산**. `q_z`는 매 샘플의 상태잡음 분산인데, 관측되는 모델 잔차는 beat 한 개 길이 `L` 동안 누적된 편차다. random walk의 누적 분산은 `L·q_z`이므로 `L`로 나눠야 한다. 이 정규화를 빼면 `q_z`가 수백 배 과대추정되어 필터가 사실상 평활화를 하지 않는다 |

**입력 5 dB에서 +5.9 dB → +16.2 dB.**
자가진단 절차가 없었다면 "Sameni는 우리 조건에 맞지 않는다"는 **틀린 결론**을 냈을 것이다.

## F-5. SWT의 σ를 level별 MAD로 추정하면 안 된다 — 초기 설계 권고를 정정

`fs = 250 Hz`, 입력 10 dB(AWGN)에서 실측한 대역별 SNR:

| Band | 대역 | band SNR | `MAD(D_j) / 참 σ` |
|---|---|---|---|
| D1 | 62.5–125 Hz | **−13.2 dB** | 1.00 |
| D2 | 31–62.5 | +0.5 dB | 1.05 |
| D3 | 15.6–31 | +12.3 dB | 1.23 |
| D4 | 7.8–15.6 | +17.5 dB | **2.01** |
| D5 | 3.9–7.8 | +18.5 dB | **4.98** |

**D3 이상은 ECG가 계수를 지배한다.** 그 대역의 MAD는 잡음이 아니라 ECG를 재고,
σ를 2~5배 과대추정해서 threshold가 QRS를 잘라낸다.

검증 세트 `snr_imp_scaled`: **전역 σ(D2) +11.4 dB vs level별 σ −3.5 dB.**

## F-4. `filtfilt` 기본 padlen으로는 front-end 자체가 11.9 dB 왜곡

0.5 Hz HPF는 수 초간 링잉하는데 scipy의 기본 padlen은 15 샘플(60 ms)이다.
padlen을 명시하고, 별개로 **평가 guard band 5 s**를 전 방법 공통 규약으로 확정했다(4.2-(4)).

## F-3. 250 Hz에서 1 샘플(4 ms) 정렬 오차가 SNR 추정을 6 dB 왜곡

QRS는 σ가 10 ms 수준이라 1 샘플만 어긋나도 beat 잔차가 부풀어 오른다.
4배 업샘플 후 template 교차상관 정렬을 필수 단계로 넣었다(20 dB → 14 dB였던 것이 20.5 dB로).

## F-1. `motion_synth`가 드물게 전(全) 0 잡음 생성

이벤트가 창 끝에 놓이면 상승항이 0이라 기여가 없다. 약 1/2600 window 확률로 발생해
**학습이 매번 에폭 6 부근에서 죽었다.** 회귀 테스트를 400 → 8000 시행으로 강화했다
(400 시행으로는 잡히지 않는다).

---

# 7. 한계와 다음 단계

## 7.1 이 결과의 적용 범위

| 한계 | 설명 |
|---|---|
| **두 축 모두 공개 데이터** | D0(합성)·D1(MIT-BIH) 둘 다 실측 장비 신호가 아니다. RQ6(device transfer)는 하드웨어 수집 대기 |
| **D1 의 참조가 `FE(원본)` 이다** | MIT-BIH 원본이 clean 이 아니므로 참조를 대역제한 신호로 정의했다(D-3). 따라서 D1 의 결론은 **"0.5~100 Hz 대역의 복원"** 에 대한 것이고, 그 대역 밖의 성분에 대해서는 아무 말도 하지 않는다 |
| **손실을 L1 로 고정** | 주 실험의 딥러닝은 전부 L1 이다. ablation 은 D1 고 SNR 에서 L3 가 유의하게 낫다고 말한다(5.8.7). 사후 선택을 피하려 재실행하지 않았으므로, **딥러닝 수치는 최선이 아니라 사전 고정된 설정의 값**이다 |
| **표본 크기** | n = 22 record. D0 의 주요 비교는 effect r = 0.99~1.00 이라 표본 수에 걸려 있지 않으나, **D1 의 `snr_imp_scaled` 는 effect r 이 0.4~0.5 로 작아 표본을 늘리면 판정이 바뀔 수 있다** |
| **학습 예산** | 전 모델이 early stopping(patience 12)으로 끝났다. D1 이 D0 보다 늦게 수렴한다(46 vs 21 epoch). 더 긴 학습에서 순위가 바뀔 가능성은 배제할 수 없다 |
| **front-end 가 천장을 정한다** | 이 파이프라인의 결론은 "이 front-end 를 쓴다는 전제" 아래에서만 성립한다. D1 에서는 참조가 front-end 출력이므로 이 전제가 더 강하게 작용한다(F-15) |
| **`qrs_dur_err_ms` 를 D1 에서 쓸 수 없다** | 지표 분해능이 28.07 ms 로, 방법 간 폭(3.7~13 ms)보다 크다(F-16). D1 의 형태 보존 판단은 `cc`·`beat_cc`·`gain_bias` 에 의존한다 |

## 7.2 판별력이 없었던 지표 — 그리고 축을 옮기자 생긴 것

**D0 에서 `rpeak_mae_ms` 는 판별력이 없었다.** 무처리(`M00`)에서도 8.72 ms 인데
부호 있는 편향(`rpeak_bias_ms`)은 1.20 ms 뿐이다 — 체계적 편향이 아니라
검출기(xqrs)의 **무작위 산포**다(250 Hz 에서 약 2 샘플). 편향이라면 빼서 교정할
수 있지만 산포는 뺄 수 없다. 방법 간 편차 0.93 ms 가 그 산포에 묻힌다.

**D1 에서는 판별력이 생겼다.** `M08` 과 `B01` 이 `M01` 대비 유의하다.
방법 간 폭은 비슷한데(0.75 ms) **기록마다 방향이 일관**해졌기 때문이다.
MIT-BIH 는 R-peak 가 사람이 주석한 참값이라 검출기 산포가 줄어든다.

반대 방향의 사례도 있다. **`qrs_dur_err_ms` 는 D0 에서 판별력이 있었고 D1 에서
잃었다** — 분해능이 0.64 ms 에서 28.07 ms 로 44 배 커진다(F-16).

> 두 사례를 나란히 두면 요점이 보인다. **어떤 지표가 쓸 만한가는 지표의 성질이
> 아니라 지표와 데이터의 조합**이다. 한 축에서 잰 분해능을 다른 축에
> 옮겨 쓰면 안 된다.

이런 항목을 삭제하지 않고 명시하는 것이 중요하다.
"측정했으나 구분되지 않았다" 는 것도 결과다.

## 7.3 다음 단계

1. ~~**SNR 추정기 교정**~~ ✅ 완료 (F-18). 추정기의 천장을 축별로 실측했고,
   그 결과 **실측 절차가 정해졌다**: 등전위(TP) 구간 추정기 (C)를 주로 쓰되,
   `artifact_beat_frac` 으로 burst 잡음 여부를 먼저 판정하고 burst 면 (A),(B)
   를 함께 본다. (C)만 쓰면 burst 를 놓쳐 SNR 을 +4~+7 dB 과대평가한다.
   분기 규칙은 `docs/04_snr_estimator_calibration_d1.md` 의 '실기록에 적용할 때'.
2. **실측 데이터 수집** (STEP 28, `docs/08_acquisition.md` 의 S1~S6).
   ECG 뿐 아니라 **잡음만** 따로 수집하는 것이 핵심이다(device adaptation 용).
3. **그 값을 5.8.2 의 표에 대입.** 실측 SNR 구간의 행을 보면 그 장비에
   딥러닝이 값을 하는지 바로 읽힌다.
4. **EXP-F (device transfer)**: 공개 데이터 학습 모델을 그대로 적용한 경우와
   실측 잡음으로 fine-tune 한 경우를 비교.
5. **(선택) 손실을 L3 로 사전 고정한 재실행** (5.8.7). 결론의 방향은 바뀌지
   않고 교차점이 13 → 15~16 dB 로 이동하는 정도다.

---

# 8. 요약

> D0 = 합성 ECG, D1 = MIT-BIH + NSTDB 실잡음. **두 축의 답이 다른 항목이
> 이 표의 핵심이다.**

| 질문 | D0 (합성) | D1 (실데이터) |
|---|---|---|
| 어떤 기법이 최고인가? | 잡음 종류와 입력 SNR 이 정한다. −5~15 dB 는 `M08`, 20 dB 는 `M04` | **같은 구조, 좁은 범위.** 딥러닝이 앞서는 것은 **13 dB 아래**뿐 |
| 딥러닝이 고전 DSP 를 이기는가? | **전 구간에서** (`M08` vs `M01` +6.57 dB, 22 기록 전부, r=1.00) | **전 구간 평균으로는 n.s.**(+1.63, p=0.18). 구간별로는 −5/0/5 dB 에서 유의 |
| front-end 위에 딥러닝이 더하는 것은? | −5 dB 에서 +7.90 dB | **−5 dB 에서 +7.07 dB — 거의 같다.** 20 dB 에서는 −4.45 dB |
| 딥러닝이 값을 하는 구간은? | 임펄스성 artifact (front-end 위에 +11.5 dB) | **재현.** `M08` 13.69 > oracle `B01` 11.81 · `B02` 6.93 |
| 딥러닝이 해로운 구간은? | baseline wander · PLI | **재현**, 그리고 **고 SNR 전반**이 추가된다 |
| 딥러닝이 위험한가? | hallucination 없음, 오히려 가장 안전 | **재현.** `M01` 대비 2.8~3.9 배, front-end 대비 3.4~4.8 배 안전 |
| Sameni 는 쓸 만한가? | 아니다 — `M01` 과 구분 안 됨(p=0.83), 왜곡 크고 느림 | **아니다, 더 확실히** — SNR 2 dB 열세, QRS 폭 +30.4 ms 훼손(p=4e-04) |
| Sameni 가 PVC 를 훼손하는가? | 그렇다 (`V−N` = −0.065, 유일) | **아니다** (+0.014). D0 의 관측은 합성 데이터의 성질이었을 수 있다 |
| wavelet 은 어디에 두어야 하나? | 표현공간(`M08`) > 안 씀(`M06`) > 전처리(`M07`) | **순서 유지, 차이 소멸** — `M08`−`M06` 이 유의(+0.54)에서 n.s.(+0.29)로 |
| 손실 함수는? | `M06` 은 L2/L3 로 개선, `M08` 은 차이 없음 | **반대** — `M08` 이 L3 로 +1.03 dB(p=1e-03), 고 SNR 에 집중 |
| 통계적으로 유의한 개선은? | `M08`·`M06`·`B01`·`M07`·`B02`·`M04` 전부 | **oracle(`B01`·`B02`)만.** 딥러닝은 저 SNR 구간에서만 |
| 가장 값진 산출물은? | **F-1~F-18 의 방법론적 발견**과 그것을 잡아낸 검증 절차 | 같음 — D1 이 F-15~18 을 추가로 드러냈다 |

## 이 보고서를 한 문장으로

> **"어떤 denoising 기법이 최고인가" 는 잘못된 질문이다.** 잡음 종류와 입력
> SNR 을 지정해야 하고, **공통 전처리가 이미 해결한 몫을 빼고** 물어야 하며,
> **어느 데이터에서 묻는지**까지 말해야 한다. 이 프로젝트는 그 세 가지를
> 차례로 빠뜨렸다가 각각 정정했다 — 마지막 것은 D1 을 재고 나서야 드러났고,
> 합성축만 봤다면 **딥러닝을 쓸 이유가 없는 구간에 딥러닝을 권했을 것이다.**

## 방법론적 요약 — 이 프로젝트가 반복해서 겪은 것

F-8 · F-10 · F-11 · F-12 · F-15 · F-16 · F-17 은 전부 같은 모양이다.

**방법이 아니라 측정의 전제가 틀렸고, 산출물은 정상으로 보였다.**

- F-8: split 은 나눴는데 morphology 가 공유됐다
- F-10: "동일 조건" 이 선언에 그쳤다
- F-11: oracle 도 front-end 천장에 걸려 있었다
- F-12: 참조를 원본으로 뒀다
- F-15: 참조를 바꾸자 front-end 계열이 공짜 점수를 받았다
- F-16: 한 축에서 잰 지표 분해능을 다른 축에 썼다
- F-17: "참 파라미터" 가 다른 축에서는 참이 아니었다

일곱 건 모두 **p 값이 유의하고 effect size 도 컸다.** 통계는 전제가 틀렸다는
것을 알려주지 않는다. 결함을 잡아낸 것은 언제나 **"이 숫자가 이렇게 클 수
있는가" 라는 크기 감각**이었다 — 26 dB(F-8), 242 %(F-12), 147 dB(F-15),
44 배(F-16), 그리고 **천장이어야 할 값이 천장이 아닌 것**(F-17).

**뒤의 다섯 건(F-15~F-18)은 한 문장으로 묶인다.** 정의가 데이터축을 따라
움직이는데 **이름만 그대로 남았다** — 참조, 지표 분해능, "참 파라미터",
포화 임계값. 한 축에서 정한 것을 다른 축에 그대로 쓰면 그 이름은 거짓이 된다.

### F-18 은 성격이 하나 다르다

앞의 일곱 건은 **틀린 값을 만들었다.** F-18 은 그렇지 않다 — 값도 코드도
정상이었고, 대신 **경고 장치가 한 번도 울리지 않았다.** 임계값(22 dB)이 어느
축의 천장보다도 높아서, 그 장치는 존재하기만 하고 아무 일도 하지 않았다.

즉 **옳은 값을 내되 그것이 한계에 닿았다는 사실을 숨겼다.** 실기록에서
추정치가 이미 천장인데 경고가 조용한 것이 그 모습이다.

> 테스트는 "경고가 뜨는 조건에서 뜨는가" 를 확인하지 **"실제 데이터에서 뜬
> 적이 있는가"** 를 확인하지 않는다. 이 프로젝트의 검사들도 그랬다.
>
> 진단 질문: **이 경고가 마지막으로 뜬 것이 언제인가.** 한 번도 없다면
> 임계값이 틀렸거나 그 경고가 불필요한 것이다. 둘 다 고쳐야 할 상태다.
