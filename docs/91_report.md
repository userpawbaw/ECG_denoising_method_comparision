# 91. 프로젝트 종합 보고서

> 대상: 이 실험을 처음 보는 평가자
> 전제 지식: 학부 신호처리 + 기초 기계학습
> 이 문서는 **왜 그렇게 설계했는가**에 초점을 둔다. 실행 방법은 `README.md`,
> 단계별 절차는 `docs/02_procedure.md`, 자동 생성 결과표는 `docs/90_results.md` 참조.

---

# 1. 문제 정의

## 1.1 출발점

자체 제작한 아날로그 프런트엔드(Arduino + AD8232 계열)로 ECG를 취득했으나,
아날로그 단에서 잡음을 줄인 뒤에도 잔여 잡음이 남는다. 오실로스코프 화면에서
광대역 잡음과 **순간적인 큰 spike**가 함께 관측되었다.

초기 접근은 Sameni의 model-based Kalman filtering이었으나 dB 개선이 기대에 못 미쳤고,
"우리 신호는 약 15 dB인데 논문은 0~10 dB에서 개선이 두드러진다"는 해석 하에
다른 기법을 탐색하려는 상황이었다.

## 1.2 문제를 다시 정의한 이유

위 상황에는 **검증되지 않은 전제가 세 개** 있다.

| 전제 | 검증했는가 | 실제 |
|---|---|---|
| "우리 신호는 15 dB다" | ✗ | clean reference가 없으면 SNR을 직접 계산할 수 없다. 정의부터 필요하다 |
| "Sameni 방식이 우리 조건에 안 맞는다" | ✗ | 구현이 올바른지 먼저 증명해야 한다 |
| "SNR을 올리면 좋은 것이다" | ✗ | 과도한 평활화가 SNR을 올릴 수 있다. 목표와 지표가 어긋난다 |

따라서 목표를 다음과 같이 다시 세웠다.

> **ECG morphology를 보존하면서 acquisition noise를 감소시키는 디지털 후처리 기법들을
> 동일한 조건에서 정량 비교하고, 실제 취득 환경의 잡음 조건에서 어떤 기법이 적합한지 규명한다.**

의도적으로 **하지 않는** 주장:
- "임상 진단 가능한 병원급 ECG를 복원한다" — 검증 불가능하고 규제 대상 주장이다.
- "의료기기를 개발했다" — 본 과제는 연구용 신호처리 시스템이다.

## 1.3 연구 질문

| RQ | 질문 | 답하는 실험 |
|---|---|---|
| RQ1 | 입력 SNR에 따라 어떤 기법이 유리한가? 특히 실측 조건에서는? | EXP-A |
| RQ2 | 잡음 **종류별**로 기법의 강약이 갈리는가? | EXP-B |
| RQ3 | 각 기법은 **깨끗한 신호에 해를 끼치는가**? | EXP-C |
| RQ4 | wavelet을 **전처리**로 쓰는 것과 **신경망의 표현공간**으로 쓰는 것 중 무엇이 나은가? | EXP-D |
| RQ5 | 딥러닝이 **없는 파형을 만들어내지는** 않는가? 부정맥을 훼손하지 않는가? | EXP-E |
| RQ6 | 공개 데이터로 학습한 모델이 자체 장비로 전이되는가? | EXP-F (실측 대기) |

---

# 2. 설계 원칙

구현에 들어가기 전에 네 가지 원칙을 정하고, 이후 모든 결정을 여기에 맞췄다.

## 원칙 1 — 평가를 기법보다 **먼저** 만든다

평가 코드를 나중에 만들면, 이미 구현한 기법의 결과를 보면서 무의식적으로
자기 기법에 유리한 지표와 조건을 고르게 된다. 그래서 순서를 강제했다.

```
PHASE 2 (평가 엔진)  →  PHASE 3 (고전 기법)  →  PHASE 4 (모델 기반)  →  PHASE 6-7 (딥러닝)
```

## 원칙 2 — baseline을 약하게 두지 않는다

"우리 기법이 X를 이겼다"는 주장은 X가 제대로 구현되었을 때만 성립한다.
초기 관찰이 "Sameni가 잘 안 된다"였으므로, **Sameni를 비교 실험에 넣기 전에
6항목 자가진단을 통과시키는 것**을 착수 조건으로 걸었다 (`docs/06_sameni_diagnosis.md`).

결과적으로 이 절차가 **구현 버그 2건**을 찾아냈고, 성능이 입력 5 dB에서
**+5.9 dB → +16.2 dB**로 바뀌었다. 이 검증이 없었다면 "Sameni는 우리 조건에서 무력하다"는
**틀린 결론**을 냈을 것이다.

## 원칙 3 — 상한선을 함께 그린다

"기법 A가 +8 dB를 냈다"는 말은 그 자체로 해석되지 않는다. 8 dB가 좋은 것인가?
더 짜낼 여지가 있는가? 이에 답하려면 **도달 가능한 최대치**가 필요하다.

| bound | 정의 | 답하는 질문 |
|---|---|---|
| `B01` oracle wavelet | clean 계수를 알고 각 wavelet 계수를 최적으로 keep/kill | wavelet thresholding으로 더 갈 수 있는가? |
| `B02` oracle Wiener | 참 PSD를 알고 `H(f)=Sx/(Sx+Sn)` 적용 | 선형 시불변 필터로 충분한가? |
| `M00` identity | 아무 처리도 안 함 | 처리가 도움이 되긴 하는가? |

두 oracle은 실제로 쓸 수 없다(clean을 참조한다). 그래서 이름이 `oracle_`로 시작하도록
코드에서 강제했고, 그런 방법만 `ctx['x_clean']`을 받을 수 있다.

## 원칙 4 — 재현 가능해야 한다

- 난수는 전역 seed가 아니라 `(experiment, record, window)`에서 **결정론적으로 유도**한다.
- 실험 1개 = YAML 1개.
- 모든 산출 디렉터리에 `manifest.json`(git hash, 패키지 버전, config 사본)을 남긴다.
- 표와 그림은 `results/`만 읽어서 재생성된다 — **원본 데이터에 다시 접근하지 않는다.**

---

# 3. 평가지표 — 왜 이것들인가

이 장이 보고서의 핵심이다. 지표를 잘못 고르면 이후 모든 실험이 무의미해진다.

## 3.1 "좋은 denoising"에는 서로 충돌하는 두 목표가 있다

clean ECG를 `x(t)`, 잡음 섞인 관측을 `y(t) = x(t) + n(t)`, 알고리즘 출력을 `x̂(t)`라 하자.

- **목표 A — 잡음 제거**: `x̂`에서 `n`의 흔적이 사라져야 한다.
- **목표 B — 신호 보존**: `x̂`가 `x`의 형태를 유지해야 한다.

두 목표는 충돌한다. 극단적으로, `x̂ = 0`은 잡음을 완벽히 제거하지만 신호도 없앤다.
반대로 `x̂ = y`는 신호를 완벽히 보존하지만 잡음도 그대로다.

ECG에서 이 충돌은 구체적이다. **QRS complex는 신호에서 가장 고주파 성분**이므로,
"고주파 = 잡음"이라고 가정하는 순간 QRS부터 깎인다. 실제로 본 실험에서
Savitzky-Golay 필터는 **깨끗한 신호에 통과시켰을 때조차 R-peak 진폭을 24.8% 감소**시켰다.

따라서 지표를 하나로 요약하려는 시도를 포기하고, **네 개 층으로 나눠 각 층이 서로 다른
질문에 답하도록** 설계했다.

```
층 1. 재구성 오차   — x̂ 가 x 에서 수치적으로 얼마나 벗어났는가
층 2. 형태 보존     — ECG 로서 중요한 구조가 유지되었는가
층 3. 하류 과제     — 이 신호로 실제 분석(심박 검출)이 되는가
층 4. 주파수 구조   — 어떤 주파수 성분을 없앴는가
```

---

## 3.2 층 1 — 재구성 오차

### 3.2.1 DC 규약 (모든 지표 공통)

**모든 지표는 `x`와 `x̂` 양쪽의 평균을 제거한 뒤 계산한다.**

이유: (1) ECG의 절대 전위 기준선은 임의값이고, 공통 front-end HPF가 이미 DC를 제거한다.
(2) 이렇게 해야 SNR/RMSE/PRDN/CC의 정의가 서로 모순되지 않는다.
느린 baseline wander 잔차는 평균 제거로 숨지 않으므로 평가력은 유지된다.

### 3.2.2 SNR을 **세 개**로 나눈 이유 — 본 프로젝트의 핵심 설계 결정

교과서적 정의는 이렇다.

```
SNR_in  = 10 log10( P(x) / P(y − x) )
SNR_out = 10 log10( P(x) / P(x̂ − x) )
SNR improvement = SNR_out − SNR_in
```

여기에 **함정**이 있다. 다음 세 종류의 방법은 **구조적으로 출력 진폭을 줄인다.**

| 방법 | 진폭이 줄어드는 이유 |
|---|---|
| soft threshold | `sign(d)·max(|d|−λ, 0)` — 살아남은 계수에서도 항상 `λ`만큼을 뺀다 |
| Kalman/EKS | process noise `Q` 설정에 따라 상태가 평균 쪽으로 눌린다 |
| MSE 학습 DL | 불확실한 구간에서 조건부 평균으로 수축한다 |

이 상태로 위 식을 그대로 쓰면, **"파형은 정확한데 크기만 3% 작은 출력"이
"파형이 3% 틀어진 출력"과 동일하게 처벌된다.** 방법 간 순위가 실제로 뒤집힌다.

그래서 세 값을 **항상 함께** 리포트한다.

| 이름 | 정의 | 답하는 질문 |
|---|---|---|
| `snr_out_strict` | `x̂` 그대로 | 그대로 써도 되는가 (실사용 기준) |
| `snr_out_scaled` | 최적 이득 `α* = ⟨x,x̂⟩/⟨x̂,x̂⟩` 보정 후 | 파형 구조 자체는 맞는가 (알고리즘 기준) |
| `gain_bias` | `α*` 값 자체 | 진폭이 얼마나 눌렸는가 |

**`gain_bias` 읽는 법**: `α* = 1.04`면 출력이 참값보다 약 4% **작다**는 뜻이다
(4% 키워야 오차가 최소가 되므로). 1보다 작으면 출력이 크다는 뜻이다.

**단위 테스트로 이 분리를 강제했다**: `x̂ = 0.5x`일 때
`snr_out_strict = 10log10(4) = 6.02 dB`, `snr_out_scaled = ∞`, `gain_bias = 2.0`.

#### 이 분리가 실제로 무엇을 잡아냈는가

`gain_bias` (1보다 크면 출력이 작다):

| 입력 SNR | `M00` identity | `M02` SavGol | `M05` EKS | `M04` SWT | `M07` |
|---|---|---|---|---|---|
| −5 dB | 0.241 | 0.723 | 0.832 | 0.692 | 1.014 |
| 20 dB | 0.990 | **1.035** | **1.043** | 1.000 | 1.004 |

- 20 dB에서 SavGol은 출력이 **3.5% 작고**, EKS는 **4.3% 작다**. 체계적 수축이 실재한다.
- `M04`(SWT garrote)는 1.000 — garrote를 고른 이유가 여기서 확인된다.
  (soft threshold를 쓴 초기 설정에서는 `gain_bias`가 1.6까지 갔다.)

`M00`(무처리)의 `gain_bias`가 −5 dB에서 0.241인 것도 중요한 정보다.
잡음이 신호보다 훨씬 크므로, 관측 신호를 0.24배로 줄이는 것만으로 오차가 줄어든다.
이것이 **스칼라 Wiener 이득**이고, 그 결과 `M00`의 `snr_imp_scaled`가 −5 dB에서
0이 아니라 **+6.2 dB**로 나온다.

> **해석 시 반드시 주의**: `snr_imp_scaled`는 identity에 무료 보너스를 준다.
> 그 크기는 `10log10(1+ρ) − SNR_in` (ρ = 선형 SNR)으로 정확히 계산되며,
> SNR이 높아질수록 0으로 수렴한다 (20 dB에서 +0.045 dB).
> **따라서 모든 곡선은 `M00` 선을 기준선으로 두고 읽어야 한다.**

### 3.2.3 RMSE와 PRDN

```
RMSE = sqrt( mean( (x − x̂)² ) )                            [mV]
PRDN = 100 · sqrt( Σ(x − x̂)² / Σ(x − x̄)² )                 [%]
```

- **RMSE**는 물리 단위(mV)라 직관적이지만 신호 진폭에 의존한다.
  진폭이 큰 기록과 작은 기록을 섞어 평균내면 큰 기록이 지배한다.
- **PRDN**은 신호 에너지로 정규화해 그 문제를 없앤다. ECG 압축/재구성 문헌의 표준 지표다.

문헌에는 `PRD1`(원신호 기준)과 `PRDN`(평균 제거 기준)이 혼재한다.
MIT-BIH 원신호에는 ADC offset이 얹혀 있어 `PRD1`은 실제 오차를 **항상 과소평가**한다.
**본 프로젝트는 `PRDN`만 쓰고 표기도 고정했다.**

**중요**: PRD는 흔히 "morphology 지표"로 소개되지만 **정확하지 않다.**
PRD는 정규화된 **전체 재구성 오차**다. morphology 왜곡이 그 오차에 반영될 뿐,
morphology를 직접 재는 것이 아니다. 그래서 별도의 형태 지표가 필요하다(층 2).

---

## 3.3 층 2 — 형태 보존

### 3.3.1 전체 파형 상관 `cc`

```
cc = Pearson correlation( x − x̄ , x̂ − x̂̄ )
```

RMSE와 상호보완적이다.

- RMSE는 **값의 정확성**을 잰다 (진폭이 절반이면 크게 벌한다).
- `cc`는 **모양의 유사성**을 잰다 (`x̂ = 0.5x`여도 `cc = 1.0`).

둘을 함께 보면 "모양은 맞는데 크기가 틀렸다"와 "크기는 맞는데 모양이 틀렸다"를 구분할 수 있다.

### 3.3.2 beat template 상관 `beat_cc` — 정렬 기준이 결정적이다

전체 파형 상관은 baseline 같은 저주파 성분이 지배해 **개별 심박의 형태 손상을 놓친다.**
그래서 R-peak를 기준으로 beat를 잘라 평균한 **template**끼리 비교한다.

여기서 **정렬 기준을 어떻게 잡느냐가 결정적**이다.

| 정렬 기준 | 문제 |
|---|---|
| `x̂`의 자체 검출 R-peak | 타이밍 오차가 상쇄되어 사라진다 → **부정확** |
| **clean reference의 R-peak (채택)** | 타이밍 오차가 상관에 반영된다 → 정확 |

본 실험은 **항상 clean reference의 R-peak로 양쪽 모두를 자른다.**

### 3.3.3 R-peak 진폭 오차 `r_amp_err_pct`

R-peak 근방 ±40 ms에서의 부호 있는 극값을 `x`와 `x̂`에서 각각 구해 상대오차를 계산한다.
±40 ms 창을 쓰는 이유는 **미세한 타이밍 이동과 진폭 감쇠를 분리**하기 위해서다
(타이밍은 `rpeak_mae_ms`가 따로 담당한다).

이 지표는 **과도한 평활화를 가장 직접적으로 잡아낸다.**

### 3.3.4 QRS duration 오차 `qrs_dur_err_ms`

NeuroKit2의 DWT 기반 delineation으로 QRS onset/offset을 찾아 폭을 비교한다.
QRS는 ECG에서 가장 빠르게 변하는 구간이므로, 공격적인 filtering이 QRS를 뭉개면
폭이 넓어지거나 좁아진다.

**단, 이 지표는 검출 알고리즘 자체의 불안정성을 포함한다.** 따라서 무조건 믿으면 안 되고,
아래 3.6의 "지표 자체의 분해능"을 먼저 재야 한다.

---

## 3.4 층 3 — 하류 과제(downstream task)

### 3.4.1 왜 필요한가

층 1·2는 모두 clean reference를 요구한다. 그런데 **실측 신호에는 clean이 없다.**
또한 궁극적으로 우리가 원하는 것은 "숫자가 좋은 파형"이 아니라
"실제 분석에 쓸 수 있는 파형"이다.

그래서 **denoising 결과에 심박 검출기를 돌려 그 성능을 잰다.**

```
Se  = TP / (TP + FN)      실제 심박을 얼마나 놓치지 않았는가
PPV = TP / (TP + FP)      검출한 것 중 진짜가 얼마나 되는가
```

### 3.4.2 공정성 규약

**모든 방법의 출력에 동일한 검출기(`wfdb.processing.xqrs_detect`)를 적용한다.**
방법마다 다른 검출기를 쓰면 최종 차이가 denoising 때문인지 검출기 때문인지 알 수 없다.

매칭은 **허용오차 ±75 ms 내 1:1 최소비용 매칭**(Hungarian)으로 한다.
그리디 매칭은 허용오차 안에 검출이 2개 들어올 때 지표를 부풀린다.

### 3.4.3 심박수 오차 `hr_err_bpm`

RR 간격의 **중앙값**으로 심박수를 구해 비교한다(평균은 이상치에 취약).
가장 직관적인 지표라 발표에서 유용하다.

---

## 3.5 층 4 — 주파수 구조

```
psd_logdist = mean( | 10log10 PSD(x) − 10log10 PSD(x̂) | )    [1–100 Hz]
band_power_err : 대역별 상대 파워 오차
```

**주의**: "ECG = 0.5–40 Hz, 그 위는 잡음"처럼 대역을 임의로 잘라 판정하지 않는다.
QRS에는 상당한 고주파 성분이 있다. **항상 reference PSD와 비교한다.**

PSD는 시간 영역에서 보이지 않는 것을 보여준다 — 어떤 방법이 60 Hz를 지웠는지,
어떤 방법이 QRS의 고주파까지 함께 잘라냈는지.

---

## 3.6 지표 자체의 분해능 — 이 단계를 건너뛰면 결과표가 무의미해진다

**방법 간 차이가 지표 자체의 흔들림보다 작으면 그 표는 아무것도 말하지 않는다.**

그래서 기법을 만들기 전에, clean 신호에 **무시할 수준의 교란(40 dB AWGN)**을 10개 seed로
주고 각 지표가 이상값에서 얼마나 흔들리는지 측정했다 (`docs/03_metric_floor.md`).

| 지표 | floor (p95) | 해석 |
|---|---|---|
| `rpeak_mae_ms` | ≈ 0 | 타이밍 계열은 해상도가 높다 |
| `hr_err_bpm` | 0 | 상동 |
| `qrs_dur_err_ms` | 0.64 ms | 이보다 작은 차이는 delineator의 불안정성 |
| `cc` | 5.0e-05 | 1에 붙어 있어 소수 4~5자리까지 봐야 한다 |
| `beat_cc` | 3.6e-07 | 매우 민감 |
| `psd_logdist` | **6.45 dB** | **매우 크다** |

**`psd_logdist`의 floor가 6.45 dB나 되는 것이 중요한 발견이다.**
ECG 파워가 거의 없는 주파수 구간에서 log-PSD 차이가 폭발하기 때문이다.
따라서 이 지표는 **절대값으로 해석하면 안 되고 방법 간 상대 비교로만** 쓴다.
PSD는 그림으로 보는 것이 주 용도다.

결과표에서는 floor보다 작은 값에 `*`를 붙여 **"구분 불가"**임을 명시한다.

---

## 3.7 ground truth가 없는 실측 신호용 지표

실측 Arduino 신호에는 clean이 없다. 그런데 "우리 신호가 정말 15 dB인가"에는 답해야 한다.
**답할 수 있다.** beat의 반복성을 이용한다.

### 3.7.1 세 가지 추정기

| 기호 | 방법 | 핵심 가정 |
|---|---|---|
| (A) | beat 평균 잔차: `m = mean(beats)`를 signal, `beat_i − m`을 noise로 | beat 형태 반복 + **잡음이 beat 간 무상관** |
| (B) | half-sample consistency: 홀수 beat 평균과 짝수 beat 평균의 상관 | 상동 (잡음 스펙트럼 가정 없음) |
| (C) | 등전위(TP) 구간 분산 | TP 구간이 평탄 + 잡음이 정상(stationary) |

(B)의 유도가 깔끔하다. 두 template을 `m + e_O`, `m + e_E`라 하면
(`e_O ⊥ e_E`, `P(e) = Pn/(N/2)`)

```
corr = Ps / (Ps + Pn')  = r' / (1 + r')      →   r' = corr / (1 − corr)
단일 beat SNR  r = r' / (N/2)
```

### 3.7.2 필수 전처리 — sub-sample 정렬

`fs = 250 Hz`에서 1 샘플은 4 ms다. QRS는 σ가 10 ms 수준이므로
**1 샘플 정렬 오차만으로 beat 잔차가 부풀어 SNR을 6 dB 이상 과소평가**한다.
실측: 참 SNR 20 dB인 신호를 **14 dB**로 추정했다.

→ 4배 업샘플 후 template 교차상관으로 정렬하는 것을 필수 단계로 넣었다.
수정 후 20 dB → **20.5 dB**로 추정된다.

### 3.7.3 가정 위반을 숨기지 않고 진단한다

(A)와 (B)는 잡음이 beat 간 무상관이라고 가정한다. 근전도 burst나 전극 움직임처럼
여러 beat에 걸쳐 상관을 갖는 잡음에서는 **SNR을 크게 과대평가**한다.

그래서 두 개의 진단 지표를 함께 낸다.

| 진단 | 의미 |
|---|---|
| `beat_resid_lag1_corr` | 인접 beat 잔차의 상관. > 0.15면 (A)(B)는 낙관적 |
| `artifact_beat_frac` | 형태 이상치 beat 비율. 크면 burst/임펄스형 → SNR을 단일 스칼라로 말하면 안 됨 |

실측 검증 (`docs/04_snr_estimator_calibration.md`): motion 잡음에서 `lag1 = 0.42`,
baseline wander에서 0.27로 **진단이 실제로 작동한다.** AWGN에서는 0.00이다.

또한 **잡음이 전혀 없어도 추정치가 무한대가 되지 않는다.** RR 변동에 따라 P/T 파 위치가
실제로 움직이므로 약 **22 dB에서 포화**한다. `ceiling_warning` 플래그로 알린다.

### 3.7.4 교정표를 미리 만들어 둔 이유

참 SNR을 아는 합성 데이터로 각 추정기의 편향을 미리 재 두었다.
그래야 실측값 "약 15 dB"를 정직하게 해석할 수 있다.
front-end 적용 후 AWGN 조건에서 편향은 **0.9 dB**다.

---

## 3.8 최종 지표 구성

**주 지표 5개** (결과표 본문)

| 지표 | 방향 | 답하는 질문 |
|---|---|---|
| `snr_imp_scaled` | ↑ | 잡음을 얼마나 제거했나 |
| `rmse` | ↓ | 참값에서 수치적으로 얼마나 벗어났나 |
| `cc` | ↑ | 파형 모양을 유지했나 |
| `rpeak_mae_ms` | ↓ | 심박 타이밍을 유지했나 |
| `qrs_dur_err_ms` | ↓ | QRS 형태를 유지했나 |

**보조 지표 8개**: `snr_imp_strict`, `gain_bias`, `prdn`, `r_amp_err_pct`,
`beat_cc`, `hr_err_bpm`, `psd_logdist`, `rtf`

**종합 점수를 만들지 않았다.** `0.4·SNR + 0.3·CC + 0.3·QRS` 같은 가중합은
가중치의 근거를 댈 수 없다. 대신 **Pareto 관점**으로 본다(4.4).

---

# 4. 실험 설계 — 왜 이 절차인가

## 4.1 신호 규격을 먼저 못박는다

| 항목 | 값 | 근거 |
|---|---|---|
| `fs` | **250 Hz** | Nyquist 125 Hz. 취득계가 이미 그 이상을 담지 못한다. 계산량 대비 최적 |
| window | **1024 = 4.096 s** | 정상 심박에서 4~6 beat 포함. `2^10`이라 SWT level 5가 안전 |
| hop | **512 (50 % overlap)** | Hann√ 창으로 정확 재구성 |
| 단위 | **mV** | R-peak 진폭 지표를 물리적으로 해석하기 위해 |

## 4.2 공정성 규약 5개 — 이것이 없으면 비교가 성립하지 않는다

### (1) 동일 입력

모든 방법이 **완전히 동일한 `y`**를 받는다. 평가 세트를 고정 시드로 미리 생성해 공유한다.

### (2) SNR sweep에서 잡음 실현을 고정

하나의 (record, segment, 잡음 조건)에 대해 잡음을 **한 번만** 뽑고 **스케일만** 바꾼다.
SNR마다 다른 잡음을 뽑으면 성능-입력SNR 곡선이 **'잡음 조성 변화'와 뒤섞여 해석 불가능**해진다.

### (3) 공통 front-end

```
FE1  zero-phase Butterworth HPF, 4차, 0.5 Hz     (baseline wander)
FE2  zero-phase Butterworth LPF, 4차, 100 Hz     (대역 제한)
FE3  IIR notch 60/120 Hz                          ← PSD로 PLI 존재를 판정한 뒤에만
```

- `filtfilt`(zero-phase)를 쓰므로 위상 왜곡이 없다 → R-peak timing이 밀리지 않는다
  (실측 편향 < 0.2 ms).
- **FE3를 무조건 적용하지 않는다.** 59–61 Hz 대역 파워가 인접 배경대역 중앙값의
  10배 이상일 때만 건다. 깨끗한 ECG의 하모닉 선구조에 오작동하지 않음을 확인했다
  (합성 ECG에서 ratio 1.0~1.6).

### (4) guard band — 실측으로 결정

zero-phase HPF는 경계에서 수 초간 링잉하고, Kalman은 수렴 시간이 필요하며,
wavelet/OLA는 패딩 경계 효과가 있다. 이를 방치하면 **깨끗한 신호에 front-end만 걸어도
왜곡이 11.9 dB**로 측정된다.

| guard [s] | front-end distortion [dB] |
|---|---|
| 0 | 11.5 |
| 1 | 25.9 |
| 3 | 40.9 |
| **5** | **41.9** (포화) |
| 10 | 41.7 |

→ **평가 구간 60 s, 양끝 5 s를 지표 계산에서 제외**로 확정.
**방법에는 전체 구간을 주고 지표만 내부에서 계산한다.** 모든 방법에 동일 적용.

### (5) 동일한 R-peak 검출기

모든 방법의 출력에 같은 검출기를 쓴다(3.4.2).

## 4.3 데이터 설계

| 세트 | 구성 | 용도 |
|---|---|---|
| **D0** 합성 | McSharry/Sameni ODE. 기록마다 다른 커널 | 파이프라인 검증, EKF 정답 검증, **외부 데이터 없이 전 실험 수행** |
| **D1** MIT-BIH + NSTDB | DS1/DS2 inter-patient split | 주 벤치마크 (외부 데이터 확보 후) |
| **D2** MIT-BIH clean + Arduino 잡음 | 장비 고유 잡음 합성 | device adaptation |
| **D3** Arduino 실측 | ground truth 없음 | 최종 real-world 평가 |

**본 보고서의 결과는 D0 기준이다.** D1은 외부 데이터 확보 후 동일 스크립트로 재실행한다.

### D0에 McSharry ODE를 쓴 이유

```
θ̇ = ω
ż = −Σᵢ (αᵢω/bᵢ²) Δθᵢ exp(−Δθᵢ²/2bᵢ²)
```

이 모델은 **Sameni EKF의 상태방정식과 정확히 같은 파라미터화**를 쓴다. 따라서
생성에 쓴 참 파라미터를 EKF에 그대로 주면 EKF는 이론적 최적에 도달해야 한다.
**도달하지 못하면 구현 버그다.** 즉 D0가 EKF의 정답 검증 케이스 역할을 한다.

### split 설계

- **record 단위 split** — window 단위로 나누면 같은 환자의 같은 morphology가
  train/test 양쪽에 들어간다.
- **잡음도 시간축 disjoint 분할** — NSTDB의 `bw/ma/em`은 각 30분짜리 기록 3개뿐이라
  랜덤 crop을 그냥 하면 train과 test가 같은 잡음 파형을 본다(noise leakage).

## 4.4 실험 매트릭스

| ID | 목적 | 규모 | 답하는 RQ |
|---|---|---|---|
| **EXP-A** | 입력 SNR sweep | 22 record × 2 seg × 6 SNR = **264 구간** × 12 기법 | RQ1 |
| **EXP-B** | 잡음 종류별 | 22 × 2 × 7 조건 = **308 구간** × 12 기법 (SNR 10 dB 고정) | RQ2 |
| **EXP-C** | distortion floor | 22 × 2 = **44 구간**, **잡음 없음** | RQ3 |
| **EXP-D** | wavelet 위치 ablation | EXP-A에서 M06/M07/M08 추출 | RQ4 |
| **EXP-E** | 안전성 프로브 | 22 구간 × 3 프로브 | RQ5 |

### EXP-C가 왜 필요한가 — 가장 저비용·고효율의 실험

**clean ECG를 그대로 각 방법에 통과시킨다.**

```
snr_distortion = 10 log10( P(x) / P(method(x) − x) )
```

이 값이 낮은 방법은 **잡음이 없어도 신호를 망가뜨린다.** 세 가지 의미가 있다.

1. delineator에 의존하지 않는 **가장 깔끔한 morphology 보존 지표**다.
2. **출력 SNR의 이론적 천장**이다. floor가 15 dB인 방법은 입력이 아무리 깨끗해도
   출력이 15 dB를 넘지 못한다.
3. 가로축 `snr_imp` × 세로축 `snr_distortion` 산점도가 곧 **Pareto front**다.

### 안전성 프로브(EXP-E) 설계

| 프로브 | 조작 | 실패 조건 |
|---|---|---|
| **P1** beat dropout | clean에서 1 beat(R ± 200 ms)를 등전위선으로 지우고 잡음을 덮음 | 그 자리에 QRS를 만들어내면 실패 |
| **P2** asystole | 3 s 구간을 등전위선으로 치환 | 심정지 구간에 beat를 생성하면 임상적으로 치명적 |
| **P3** ectopic | PVC beat만 골라 형태 보존을 정상 beat와 비교 | PVC가 정상 QRS로 '교정'되면 실패 |

`halluc_energy` = 지운 구간에서 출력이 만들어낸 에너지 / 원래 그 구간의 에너지.
**낮을수록 안전하다.**

## 4.5 통계 처리

- 집계 단위는 **record** (window는 서로 독립이 아니다 → p값이 과대해진다).
- 같은 record에 여러 방법을 적용했으므로 **paired Wilcoxon signed-rank**.
- 다중비교는 **Holm-Bonferroni** 보정.
- **효과크기(rank-biserial)를 함께 보고**한다. p값만 쓰면
  "통계적으로 유의하지만 0.2 dB 차이" 같은 무의미한 주장이 된다.

---

# 5. 결과와 해석

> ## ⚠️ 이 장의 딥러닝 수치는 현재 재실행 대기 중이다
>
> 본문 작성 후 **공정성 규약 (3) 공통 front-end 의 비대칭**이 발견됐다 (4.2 참조).
> 고전 기법은 전부 내부에서 front-end 를 적용하는데 딥러닝만 raw 입력을 받고 있었다.
> 즉 아래 표의 `M06`/`M07`/`M08` 은 **기저선 변동 제거를 처음부터 학습해야 하는
> 불리한 조건**에서 나온 값이다. 실측으로 확인한 크기:
>
> | 입력 10 dB, 잡음 종류 | front-end 단독 `M_FE` | `M06` (FE 없이 학습) |
> |---|---|---|
> | baseline wander | **+20.26 dB** | +10.58 dB |
> | PLI 60 Hz | +17.46 dB | +10.58 dB |
>
> 즉 **딥러닝이 고전 기법에 진 것처럼 보이는 조건의 상당 부분이 front-end 유무**였다.
> 이는 방법의 우열이 아니라 실험 설계의 결함이다.
>
> 조치: 학습·추론 양쪽에 동일하게 front-end 를 적용하도록 고치고 (`cfb5d1a`),
> 전 딥러닝 모델을 재학습한 뒤 EXP-A/B/C/E 를 재실행한다. **아래 수치 중
> 딥러닝이 관련된 모든 비교는 그때까지 잠정값으로 읽어야 한다.** 고전 기법
> 사이의 비교(`M01`~`M05`, `B01`/`B02`)와 3장의 지표 설계 논의는 영향받지 않는다.
>
> 함께 철회된 서술: "SWT 가 baseline wander/PLI 조건에서 oracle 에 근접한다" —
> 그 이득의 대부분은 SWT 가 아니라 front-end 가 낸 것이었다 (해당 조건에서
> SWT 의 기여분은 측정상 정확히 0 dB). 그래서 이제 모든 표에 `M_FE`(front-end
> 단독)를 필수로 넣어, 각 이득 중 얼마가 front-end 에서 온 것인지 바로 읽히게 했다.

## 5.0 비교 대상

| ID | 분류 | 방법 |
|---|---|---|
| `M00` | — | Identity (하한선) |
| `M01` | classical | Bandpass 0.5–40 Hz + 자동 notch |
| `M02` | classical | Savitzky-Golay (40 ms, 3차) |
| `M03` | time-freq | DWT soft threshold |
| `M04` | time-freq | **SWT adaptive threshold** (전역 σ + level별 k + QRS 보호 + garrote) |
| `M05f`/`M05` | model-based | **Sameni EKF / EKS** |
| `M06` | deep | **Residual 1D U-Net** (976 K params) |
| `M07` | hybrid | SWT → Residual U-Net (순차) |
| `M08` | hybrid | **Wavelet-subband Residual U-Net** (993 K params) |
| `B01`/`B02` | oracle | Oracle wavelet / Oracle Wiener (**각자 자기 계열의 상한**) |

## 5.1 RQ1 — 입력 SNR에 따라 답이 바뀐다

`snr_imp_scaled` [dB], EXP-A 264 구간 평균:

| 입력 SNR | `M00` | `M01` | `M02` | `M03` | `M04` | `M05` | `M06` | `M07` | `M08` | `B01` |
|---|---|---|---|---|---|---|---|---|---|---|
| −5 | 6.20 | 11.17 | 10.99 | 10.31 | 11.71 | 12.11 | 12.63 | **18.54** | 13.79 | 15.86 |
| 0 | 3.02 | 9.28 | 8.75 | 8.36 | 10.31 | 10.29 | 12.05 | **16.72** | 12.56 | 14.52 |
| 5 | 1.20 | 7.76 | 6.35 | 6.81 | 9.41 | 8.17 | 10.61 | **13.54** | 10.75 | 13.31 |
| 10 | 0.42 | 6.01 | 3.33 | 5.44 | 8.68 | 5.57 | 8.34 | **9.56** | 8.42 | 12.08 |
| **15** | 0.14 | 3.60 | −0.43 | 4.01 | **7.79** | 2.42 | 5.34 | 5.20 | 5.52 | 10.64 |
| **20** | 0.05 | 0.40 | −4.81 | 2.30 | **6.39** | −1.93 | 1.55 | 0.59 | 1.97 | 8.57 |

**읽는 법**:

1. **저 SNR(≤10 dB)에서는 `M07`(SWT→U-Net)이 지배적**이다.
   −5 dB에서 +18.5 dB로, oracle wavelet(15.9)보다도 높다.
   (모순이 아니다. `B01`은 **wavelet thresholding 계열**의 상한이지 모든 방법의 상한이 아니다.)
2. **고 SNR(≥15 dB)에서는 `M04`(튜닝된 SWT)가 유일하게 유지된다.**
   20 dB에서 +6.39 dB인데 2위 `M03`이 +2.30이다.
3. **`M02`와 `M05f`는 15 dB 이상에서 음수다.** 즉 **아무 처리도 안 하는 편이 낫다.**
   `M05`(EKS)도 20 dB에서 −1.93이다.
4. `M00` 곡선(6.20 → 0.05)은 3.2.2에서 설명한 스칼라 Wiener 보너스다.
   **모든 곡선을 이 선 기준으로 읽어야 한다.**

### 왜 이런 교차가 생기는가

- **모델 기반(Sameni)**은 강한 사전지식(ECG 동역학 모델)을 갖는다.
  잡음이 지배적일 때는 그 사전지식이 정보를 보태지만, 신호가 이미 깨끗하면
  **모델이 실제 파형을 자기 모델 쪽으로 끌어당긴다** → 오히려 손해.
- **딥러닝**은 학습 분포(SNR −5~20 균등)의 중심에서 강하고, 양 끝에서 약하다.
  또한 고정된 왜곡 바닥이 있어(5.3) 고 SNR에서 그 바닥에 걸린다.
- **SWT thresholding**은 threshold가 잡음 수준에 자동 적응하고
  (`σ`를 MAD로 추정), 잡음이 작으면 threshold도 작아져 **아무것도 안 하는 쪽으로 수렴**한다.
  그래서 고 SNR에서 손해를 보지 않는다.

### 통계 검정 (baseline = `M01`, paired Wilcoxon + Holm, n = 22 record)

| 방법 | Δmean [dB] | p (Holm) | effect r | 유의? |
|---|---|---|---|---|
| `B01` | +6.13 | < 1e-4 | 1.000 | ✓ (oracle) |
| `M07` | +4.32 | 1.0e-4 | 0.945 | **✓** |
| `B02` | +3.58 | < 1e-4 | 1.000 | ✓ (bound) |
| `M04` | +2.68 | 5.0e-4 | 0.874 | **✓** |
| `M08` | +2.46 | 0.101 | 0.518 | **✗** |
| `M06` | +2.05 | 0.101 | 0.542 | **✗** |
| `M03` | −0.16 | 0.849 | −0.202 | ✗ |
| `M05` | −0.27 | 0.849 | −0.170 | ✗ |
| `M02` | −2.34 | 1.0e-4 | −0.929 | ✓ (더 나쁨) |
| `M05f` | −2.52 | 9.0e-4 | −0.842 | ✓ (더 나쁨) |

> **중요**: 다중비교 보정 후 **`M06`과 `M08`은 단순 bandpass 대비 유의하지 않다.**
> 평균은 +2.0~2.5 dB 높지만 record 간 분산이 커서(n=22) 유의성에 도달하지 못했다.
> "딥러닝이 더 좋다"고 말하려면 이 결과를 반드시 함께 보고해야 한다.
> 유의한 실용 기법은 **`M04`와 `M07` 둘뿐**이다.

## 5.2 RQ2 — 잡음 종류가 답을 바꾼다 (가장 실용적인 결과)

`snr_imp_scaled` [dB], 입력 10 dB 고정, EXP-B 308 구간:

| 방법 | awgn | bw | em(motion) | ma(EMG) | pli(60 Hz) | **impulse** | mixed |
|---|---|---|---|---|---|---|---|
| `M01` | 4.06 | 11.12 | 5.46 | 5.85 | 12.25 | 1.25 | 5.85 |
| `M02` | 2.53 | 5.18 | 2.68 | 4.18 | 5.59 | 1.09 | 3.32 |
| `M03` | 2.05 | 18.52 | 7.38 | 4.73 | 11.18 | 0.48 | 5.54 |
| **`M04`** | 8.00 | **19.13** | 7.44 | 7.62 | **19.55** | **0.45** | 8.36 |
| `M05` | 3.03 | 7.94 | 4.73 | 7.82 | 8.31 | 3.34 | 5.20 |
| `M06` | 8.26 | 6.60 | 4.30 | 10.65 | 12.02 | **10.06** | 7.56 |
| `M07` | 7.62 | 10.78 | **9.83** | 9.21 | 10.81 | 9.00 | **9.15** |
| **`M08`** | 8.00 | 6.35 | 3.97 | **11.02** | 13.06 | **10.44** | 7.36 |
| `B01` | 9.67 | 19.13 | 7.51 | 13.92 | 22.38 | 8.66 | 11.49 |

**핵심 발견 — 임펄스성 artifact에서 계열이 완전히 갈린다.**

```
임펄스:  딥러닝 계열  +9.0 ~ +10.4 dB
         고전 DSP 계열 +0.45 ~ +1.25 dB     (M04 가 0.45 로 최하위)
```

**이유**: 임펄스는 시간축에서 sparse하고 주파수축에서 광대역이다.
따라서 **주파수 기반 분리가 원리적으로 통하지 않는다.**
wavelet threshold 입장에서 임펄스는 "큰 계수"라 오히려 살아남는다.
반면 신경망은 시간 영역 패턴으로 임펄스를 인식할 수 있다.

**이것이 본 프로젝트에서 딥러닝이 값을 하는 유일하게 명확한 구간이며,
사용자가 오실로스코프에서 관측한 spike가 바로 이 경우다.**

반대로:
- **PLI(60 Hz)**: `M04` +19.55로 oracle(22.38)에 근접. 딥러닝은 +12~13으로 뒤진다.
- **baseline wander**: `M04` +19.13으로 **oracle과 동일**. 딥러닝은 +6.4로 크게 뒤진다.

즉 **단일 승자는 없다.** 실제 시스템은 잡음 조성을 먼저 진단한 뒤 기법을 골라야 한다.

## 5.3 RQ3 — 깨끗한 신호에 해를 끼치는가 (EXP-C)

**잡음 없는 clean 신호를 그대로 통과시킨 결과:**

| 방법 | distortion floor [dB] ↑ | `r_amp_err` [%] ↓ | `qrs_dur_err` [ms] ↓ | `beat_cc` ↑ | `Se` ↑ |
|---|---|---|---|---|---|
| `M00` | ∞ | 0.00 | 0.00 | 1.0000 | 0.994 |
| `B01`/`B02` | 34.13 | 0.43 | 0.01 | 1.0000 | 0.994 |
| **`M04`** | **34.09** | **0.43** | **0.39** | **1.0000** | 0.994 |
| `M03` | 32.33 | 0.97 | 3.65 | 0.9999 | 0.994 |
| `M08` | 23.19 | 8.08 | 10.21 | 0.9836 | 0.953 |
| `M06` | 22.33 | 6.28 | 15.46 | 0.9892 | 0.975 |
| `M01` | 22.23 | 10.60 | 10.26 | 0.9918 | 0.994 |
| `M07` | 20.69 | 13.13 | 13.62 | 0.9730 | 0.952 |
| `M05` | 18.00 | 6.68 | **30.58** | 0.9979 | 0.993 |
| `M05f` | 15.48 | 5.13 | 19.83 | 0.9974 | 0.990 |
| **`M02`** | **15.39** | **24.85** | 14.82 | 0.9714 | 0.994 |

**해석**:

1. **`M04`는 거의 무해하다.** distortion floor 34.09 dB로 oracle(34.13)과 사실상 동일하며,
   그 34 dB는 **공통 front-end 자체의 한계**다. 즉 SWT 단계가 추가로 손상시키는 것이 없다.
   R-peak 진폭 오차 0.43 %, QRS 폭 오차 0.39 ms.
2. **`M02`(SavGol)는 깨끗한 신호의 R-peak 진폭을 24.85 % 깎는다.**
   이것이 5.1에서 고 SNR에 `M02`가 음수인 이유를 직접 설명한다.
   그리고 이 방법의 출력 SNR은 **어떤 입력에서도 15.4 dB를 넘을 수 없다.**
3. **`M05`(EKS)는 QRS 폭을 30.6 ms 왜곡한다.** 모델 사전지식이 실제 파형을
   모델 쪽으로 끌어당긴 결과다. `beat_cc`는 0.998로 높은데 QRS 폭 오차가 큰 것은,
   **평균적 형태는 맞지만 QRS 경계가 밀린다**는 뜻이다 — 두 지표를 함께 봐야 보이는 현상이다.
4. **딥러닝 계열은 깨끗한 신호에서도 심박 검출률을 떨어뜨린다.**
   `Se`가 `M07` 0.952, `M08` 0.953, `M06` 0.975로, 고전 기법(0.994) 대비 낮다.
   **잡음이 전혀 없는데도 beat를 놓친다**는 뜻이므로 실사용에서 중요한 감점 요인이다.

### Pareto front

가로축 `snr_imp_scaled`(잡음 제거), 세로축 distortion floor(신호 보존):

```
- Pareto front (실용 기법): M04, M07
- 지배당함 (두 축 모두 더 나은 대안 존재): M01, M02, M03, M05f, M05, M06, M08
```

`M04`는 보존 축의 최상단(34.09)에 있으면서 제거도 준수하고,
`M07`은 제거 축의 최상단(10.69)에 있으면서 보존은 낮다(20.69).
**둘 중 무엇을 고를지는 응용이 결정한다** — 이것이 종합 점수를 만들지 않은 이유다.

## 5.4 RQ4 — wavelet을 어디에 쓸 것인가 (EXP-D)

같은 backbone(Residual U-Net) · 같은 손실 · 같은 데이터에서 **wavelet의 위치만** 바꾼다.

| ID | wavelet의 역할 | `snr_imp_scaled` | `rmse` | `cc` | distortion floor |
|---|---|---|---|---|---|
| `M06` | 쓰지 않음 (raw 파형 입력) | 8.42 | 0.0350 | 0.9574 | 22.33 |
| `M07` | **전처리 필터** (SWT thresholding 후 U-Net) | **10.69** | **0.0256** | **0.9742** | 20.69 |
| `M08` | **입력 표현공간** (SWT subband를 채널로) | 8.83 | 0.0355 | 0.9573 | 23.19 |

**해석**:

- 전체 평균으로는 `M07` > `M08` > `M06`이다. **전처리가 이겼다.**
- 그러나 SNR별로 보면 다르다. `M07`은 저 SNR에서 압도적(−5 dB: +18.5)이지만
  15 dB 이상에서 `M08`에게 역전된다(5.20 vs 5.52).
- **보존 축에서는 순서가 뒤집힌다**: `M08` 23.19 > `M06` 22.33 > `M07` 20.69.
  즉 `M07`의 이득은 **보존을 희생해서 얻은 것**이다.
- 검증셋(1024 샘플 window 기준)에서는 `M08`(+12.11) > `M06`(+10.99) > `M07`(+7.43)으로
  순서가 달랐다. **평가 프로토콜(구간 길이·guard band)이 순위를 바꿀 수 있다**는 사례다.
  검증 지표는 체크포인트 선택용이고, 보고 대상은 테스트 프로토콜 결과다.

**결론**: "wavelet을 표현공간으로 주면 항상 낫다"는 단순한 주장은 성립하지 않는다.
`M08`은 보존이 좋고 고 SNR에 강하며, `M07`은 저 SNR 제거력이 강하다.
다만 **다중비교 보정 후 `M08`은 bandpass 대비 유의하지 않다**(5.1)는 점을 함께 봐야 한다.

## 5.5 RQ5 — 안전성 (EXP-E)

### P1/P2 — hallucination

`halluc_energy` (지운 구간에서 출력이 만들어낸 에너지 비율, **낮을수록 안전**):

| 방법 | P1 (1 beat 삭제) mean / max | P2 (3 s asystole) mean / max |
|---|---|---|
| `M00` | 0.189 / 1.331 | 0.291 / 0.730 |
| `M01` | 0.030 / 0.298 | 0.091 / 0.214 |
| `M04` | 0.030 / 0.413 | 0.091 / 0.241 |
| `M05` | 0.024 / 0.147 | 0.070 / 0.224 |
| **`M06`** | **0.009 / 0.024** | **0.017 / 0.072** |
| **`M07`** | **0.010 / 0.052** | 0.015 / 0.050 |
| `M08` | 0.020 / 0.077 | 0.028 / 0.117 |
| `B01` | 0.008 / 0.091 | 0.070 / 0.244 |

**딥러닝이 가장 안전하다.** 우려했던 "없는 beat를 만들어낸다"는 현상은 관측되지 않았다.

**이유**: 세 딥러닝 모델 모두 **residual 구조**(출력 = 입력 − 예측잡음)를 쓰고,
출력 head를 0으로 초기화해 **학습 시작 시점의 출력이 정확히 identity**가 되도록 했다.
이 설계가 hallucination 경향을 구조적으로 억제한다.

> 단, `halluc_energy`는 잡음이 그대로 남아도 올라간다.
> `M00`이 0.189인 것은 생성이 아니라 잔여 잡음이다.
> 따라서 **반드시 잡음 제거량(EXP-A)과 함께** 읽어야 한다.

### P3 — 부정맥(PVC) 훼손

| 방법 | 정상 beat `N` | PVC `V` | **V − N** |
|---|---|---|---|
| `B01` | 0.9917 | 0.9972 | +0.0054 |
| `M04` | 0.9762 | 0.9909 | +0.0147 |
| `M06` | 0.9799 | 0.9958 | +0.0159 |
| `M08` | 0.9788 | 0.9967 | +0.0179 |
| `M07` | 0.9732 | 0.9959 | +0.0227 |
| **`M05`** | 0.9824 | **0.8973** | **−0.0851** |

**`M05`(Sameni EKS)만 PVC를 훼손한다.**

이것은 우연이 아니라 **model-based 방법의 원리적 한계**다. Sameni 모델은
"위상 θ에 대해 고정된 파형"을 가정하고 그 파라미터를 정상 beat들의 평균에서 추정한다.
PVC는 그 가정을 정면으로 위반하는 beat이므로, 필터가 이를 **정상 QRS 쪽으로 끌어당긴다.**

임상적으로 중요하다. **부정맥 검출이 목적이라면 이 방법을 쓰면 안 된다.**
5.3에서 본 `M05`의 QRS 폭 왜곡 30.6 ms와 같은 원인이다.

## 5.6 계산 비용

`RTF` = 처리시간 / 신호길이 (단일 CPU 스레드, 60 s 구간):

| 방법 | RTF | 배수 |
|---|---|---|
| `M01`/`M02`/`M03` | 0.0001 | 10,000× 실시간 |
| `M04` | 0.00078 | 1,280× |
| `M06` | 0.00092 | 1,090× |
| `M08` | 0.00116 | 860× |
| `M07` | 0.0017 | 590× |
| `M05f` | 0.0124 | 81× |
| **`M05`** | **0.0161** | **62×** |

**모든 방법이 실시간 처리 가능**하다(RTF ≪ 1).

주목할 점: **Sameni EKS가 가장 느리다.** 976 K 파라미터 U-Net보다 **17배 느리다.**
Kalman 계열은 샘플마다 순차적 행렬 연산이 필요해 병렬화가 안 되는 반면,
CNN은 전체 window를 한 번에 처리하기 때문이다.
"딥러닝은 무겁고 고전 기법은 가볍다"는 통념이 여기서는 성립하지 않는다.

---

# 6. 개발 과정에서 발견한 것

결과보다 이쪽이 방법론적으로 더 값질 수 있다. 전부 실측 근거와 회귀 테스트가 있다.
번호 역순(늦게 발견한 것부터)으로 싣는다. 전체 목록은 `docs/02_procedure.md` F-1 ~ F-10.

**앞의 세 개(F-10, F-9, F-8)는 성격이 같다: 방법이 아니라 벤치마크가 틀렸던 경우다.**
셋 다 "그럴듯한 결과표"를 만들어냈고, 표만 봐서는 드러나지 않았다.

## F-10. "모든 방법에 동일 조건"이 선언에 그쳐 있었다

공정성 규약 (3)은 **모든 방법에 동일한 front-end를 준다**였다. 그런데 고전 기법은
전부 내부에서 front-end를 적용하는 반면, **딥러닝만 학습·추론 양쪽에서 raw를 받고
있었다.** 규약을 문서에 써 두고 코드에서 확인하지 않은 결과다.

입력 10 dB에서 실측:

| 잡음 종류 | `M_FE` front-end 단독 | `M04` FE+SWT | `M06` FE 없이 학습 |
|---|---|---|---|
| baseline wander | **20.26** | 20.26 | 10.58 |
| PLI 60 Hz | 17.46 | 19.41 | 10.58 |
| AWGN | 1.46 | **7.91** | 10.58 |
| impulse | 0.44 | 0.44 | 10.58 |

두 가지가 한꺼번에 드러난다.

1. **딥러닝이 진 것처럼 보이던 조건의 상당 부분이 front-end 유무였다.** 방법의
   우열이 아니라 실험 설계의 결함이다.
2. **bw/pli에서 SWT의 기여분은 정확히 0 dB다** (`M_FE` 20.26 = `M04` 20.26).
   이 문서의 초판은 "SWT가 baseline wander/PLI 조건에서 oracle에 근접한다"고
   썼는데, 그 이득은 SWT가 아니라 front-end가 낸 것이었다. **철회한다.**
   SWT가 실제로 일하는 곳은 AWGN(1.46 → 7.91)뿐이다.

> **일반화된 교훈**: 공정성 규약은 **선언이 아니라 측정으로** 확인해야 한다.
> 그래서 이제 모든 결과표에 `M_FE`(front-end 단독)를 필수 열로 넣는다.
> 각 방법의 이득 중 얼마가 공통 전처리에서 온 것인지 표 안에서 바로 읽히지 않으면,
> 같은 착각이 반복된다.

## F-9. 앞단을 고치면 뒤에 학습된 모든 체크포인트가 동시에 무효가 된다

F-10을 고친 뒤 `L1` 체크포인트만 재학습된 상태에서 loss ablation 표를 만들면
`L1(FE 있음)` vs `L3/L4(FE 없음)`을 비교하게 된다. 이 차이는 손실 함수 효과가
아니라 front-end 효과다. 게다가 그 표의 근거는 학습 로그의 **VAL** `snr_imp`였다 —
early stopping이 best를 고른 바로 그 집합이므로 낙관적으로 편향된다.

조치: 해당 STEP의 완료 표시를 되돌리고 결론을 철회했다. 재발 방지로 ablation 표
생성기가 체크포인트의 `frontend` 플래그와 **학습 시점 git hash**를 표에 함께 싣고,
설정이 섞여 있으면 경고 후 종료코드 2로 비교를 거부한다.

> **일반화된 교훈**: ablation 표는 "각 행이 언제 학습됐는가"를 기록하지 않으면
> 파이프라인 수정 이후 **조용히 거짓**이 된다. 표는 숫자만이 아니라 그 숫자가
> 만들어진 조건을 함께 실어야 한다.

## F-8. 합성 벤치마크가 딥러닝을 12 dB 과대평가하고 있었다

초기 합성 데이터 생성기는 **모든 기록에 같은 커널**을 썼다.
record 단위로 split을 해도 **morphology는 train/test가 완전히 동일**하므로,
1 M 파라미터 신경망이 생성기를 통째로 외운다.

| 조건 (입력 5 dB) | `M06` U-Net | `M04` SWT |
|---|---|---|
| 학습에 쓰인 morphology | **+16.5 dB** | +7.2 dB |
| 커널을 교란한 morphology | **+4~6 dB** | +8~12 dB |

수정 전 EXP-A 예비 실행에서 **DL +26 dB vs DSP +3 dB**가 나왔다.
이는 방법의 우열이 아니라 **벤치마크의 인공물**이었다.

> **일반화된 교훈**: split 축(record)과 **변이 축(morphology)이 일치하지 않으면
> record split은 leakage를 막지 못한다.**
> MIT-BIH에서는 기록마다 실제로 다른 환자이므로 이 문제가 없다 — 합성 데이터 고유의 함정이다.

조치: 기록(=피험자)마다 파형 위치·진폭·폭을 흔들고 낮은 확률로 T파를 역전시켰다.
기록 간 beat template 상관이 **1.0 → 0.0~0.6**으로 떨어졌고, 회귀 테스트로 고정했다.

## F-6. Sameni 구현 버그 2건 — "방법이 안 된다"가 아니라 "구현이 틀렸다"

| 버그 | 내용 |
|---|---|
| (a) | phase-averaged template의 **상수 오프셋**. 상태방정식은 `dz/dθ`만 규정하므로 오프셋은 동역학과 무관한데, 이를 방치하면 적합 R²가 0.99 → 0.82로 떨어지고 그 잔차가 `q_z` 과대추정으로 전파된다 |
| (b) | **`q_z`를 누적 분산으로 잘못 계산**. `q_z`는 매 샘플의 상태잡음 분산인데, 관측되는 모델 잔차는 beat 한 개 길이 `L` 동안 누적된 편차다. random walk의 누적 분산은 `L·q_z`이므로 `L`로 나눠야 한다. 이 정규화를 빼면 `q_z`가 수백 배 과대추정되어 필터가 사실상 평활화를 하지 않는다 |

**입력 5 dB에서 +5.9 dB → +16.2 dB.**
자가진단 절차가 없었다면 "Sameni는 우리 조건에 맞지 않는다"는 **틀린 결론**을 냈을 것이다.

## F-5. SWT의 σ를 level별 MAD로 추정하면 안 된다 — 초기 설계 권고를 정정

`fs = 250 Hz`, 입력 10 dB(AWGN)에서 실측한 대역별 SNR:

| Band | 대역 | band SNR | `MAD(D_j) / 참 σ` |
|---|---|---|---|
| D1 | 62.5–125 Hz | **−13.2 dB** | 1.00 |
| D2 | 31–62.5 | +0.5 dB | 1.05 |
| D3 | 15.6–31 | +12.3 dB | 1.23 |
| D4 | 7.8–15.6 | +17.5 dB | **2.01** |
| D5 | 3.9–7.8 | +18.5 dB | **4.98** |

**D3 이상은 ECG가 계수를 지배한다.** 그 대역의 MAD는 잡음이 아니라 ECG를 재고,
σ를 2~5배 과대추정해서 threshold가 QRS를 잘라낸다.

검증 세트 `snr_imp_scaled`: **전역 σ(D2) +11.4 dB vs level별 σ −3.5 dB.**

## F-4. `filtfilt` 기본 padlen으로는 front-end 자체가 11.9 dB 왜곡

0.5 Hz HPF는 수 초간 링잉하는데 scipy의 기본 padlen은 15 샘플(60 ms)이다.
padlen을 명시하고, 별개로 **평가 guard band 5 s**를 전 방법 공통 규약으로 확정했다(4.2-(4)).

## F-3. 250 Hz에서 1 샘플(4 ms) 정렬 오차가 SNR 추정을 6 dB 왜곡

QRS는 σ가 10 ms 수준이라 1 샘플만 어긋나도 beat 잔차가 부풀어 오른다.
4배 업샘플 후 template 교차상관 정렬을 필수 단계로 넣었다(20 dB → 14 dB였던 것이 20.5 dB로).

## F-1. `motion_synth`가 드물게 전(全) 0 잡음 생성

이벤트가 창 끝에 놓이면 상승항이 0이라 기여가 없다. 약 1/2600 window 확률로 발생해
**학습이 매번 에폭 6 부근에서 죽었다.** 회귀 테스트를 400 → 8000 시행으로 강화했다
(400 시행으로는 잡히지 않는다).

---

# 7. 한계와 다음 단계

## 7.1 이 결과의 적용 범위

| 한계 | 설명 |
|---|---|
| **합성 데이터 기준** | 본 보고서의 모든 수치는 D0(합성 ECG) 기준이다. MIT-BIH(D1)로 반드시 재검증해야 한다. 코드는 `--source mitdb`로 동일하게 동작한다 |
| **딥러닝의 분포 의존성** | 모델은 이 합성 분포에서 학습했다. F-8이 보여주듯 분포가 바뀌면 성능이 크게 달라진다 |
| **표본 크기** | n = 22 record. `M06`/`M08`이 유의성에 도달하지 못한 주된 이유다 |
| **학습 예산** | 모든 학습이 에폭 상한(60) 근처 또는 early stopping에서 끝났다. 충분히 수렴했다고 단정할 수 없다 |
| **실측 미검증** | RQ6(device transfer)는 하드웨어 수집 대기 상태다 |
| **딥러닝 수치 재실행 대기** | F-10(front-end 비대칭) 수정 후 전 모델을 재학습 중이다. 5장의 딥러닝 관련 수치는 그 전 값이므로 잠정값이다 |

## 7.2 판별력이 없었던 지표

`rpeak_mae_ms`는 무처리(`M00`)에서도 8.72 ms다. 그런데 **부호 있는 편향(`rpeak_bias_ms`)은
1.20 ms에 불과하다** — 즉 이 8.72 ms는 체계적 편향이 아니라 검출기(xqrs)의 **무작위 산포**다
(250 Hz에서 약 2 샘플). 편향이라면 빼서 교정할 수 있지만 산포는 뺄 수 없다.
방법 간 편차는 0.93 ms로 이 산포 안에 묻히므로 **이 조건에서는 판별력이 없다.**
MIT-BIH처럼 형태 변이가 큰 데이터에서 다시 볼 항목이다.

이런 항목을 삭제하지 않고 명시하는 것이 중요하다.
"측정했으나 구분되지 않았다"는 것도 결과다.

## 7.3 다음 단계

1. **MIT-BIH/NSTDB 확보 후 전 실험 재실행** (STEP 14–15).
   `docs/03_metric_floor.md`(지표 분해능)와 `docs/05_swt_tuning.md`(SWT 파라미터)도
   TRAIN split 기준으로 재측정해야 한다.
2. **실측 데이터 수집** (STEP 28, `docs/08_acquisition.md`의 S1~S6 프로토콜).
   ECG뿐 아니라 **잡음만** 따로 수집하는 것이 핵심이다(device adaptation용).
3. **실측 SNR 추정** (STEP 29). `scripts/estimate_real_snr.py`가
   세 추정기 + 진단 지표를 내고, `docs/04_snr_estimator_calibration.md`의
   편향표와 함께 해석한다.
4. **그 값을 5.1의 표에 대입.** 실측 SNR 구간의 행을 보면
   **그 장비에 어떤 기법을 써야 하는지 바로 읽을 수 있다.**
5. **EXP-F (device transfer)**: 공개 데이터 학습 모델을 그대로 적용한 경우와
   실측 잡음으로 fine-tune한 경우를 비교.

---

# 8. 요약

| 질문 | 답 |
|---|---|
| 어떤 기법이 최고인가? | **조건에 따라 다르다.** 저 SNR·임펄스 → 딥러닝, 고 SNR·PLI·baseline → 튜닝된 SWT |
| 실측 조건(≈15 dB)에서는? | **`M04`(SWT) +7.79 dB.** 딥러닝(+5.2~5.5)보다 낫다 |
| 딥러닝이 값을 하는 구간은? | **임펄스성 artifact.** +10.4 dB vs 고전 DSP 최대 +1.25 dB |
| 딥러닝이 위험한가? | hallucination은 관측되지 않았다(residual 구조 덕분). 다만 **깨끗한 신호에서도 심박 검출률을 떨어뜨린다**(Se 0.95 vs 0.99) |
| Sameni는 쓸 만한가? | 저 SNR에서는 경쟁력이 있으나 **15 dB 이상에서 해롭고, PVC를 훼손하며, 가장 느리다** |
| 통계적으로 유의한 개선은? | bandpass 대비 **`M04`와 `M07`만.** `M06`/`M08`은 다중비교 보정 후 유의하지 않다 |
| 가장 값진 산출물은? | 결과 자체보다 **F-1~F-8의 방법론적 발견**과, 그것을 잡아낸 검증 절차 |
