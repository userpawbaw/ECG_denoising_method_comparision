numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
pyarrow>=12.0
PyWavelets>=1.4
wfdb>=4.1
neurokit2>=0.2.7
tqdm>=4.65
PyYAML>=6.0
pytest>=7.4
torch>=2.0
# 실시간 시연(R-5)에서 아두이노 시리얼을 읽을 때만 필요하다.
# --replay 경로는 이것 없이도 돈다.
pyserial>=3.5

# UI/UX 개발용 검증판(UD-4). 시연 산출물에는 필요 없다 — `file://` 로 열리는
# 무빌드 HTML 이 그대로 시연에 나간다. 이것은 **화면을 열어 보는 검사**용이고,
# 없으면 `pytest -m screens` 가 실패가 아니라 skip 된다.
playwright>=1.40
