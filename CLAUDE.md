# CLAUDE.md — 명판

ECG 잡음 제거 기법 비교(졸업과제). 규약 원문은 `docs/`, **이 파일은 조종석
명판**이다 — 행동 직전에 볼 것만 담는다. **상한 60 줄.** 늘어나면
`docs/17_checklists.md` 로 내린다.

## 항상

- 답변은 **한국어**, `~했다` 체. `[사용자 지시]`
- `data/arduino/` 는 **git 에 넣지 않는다** — 개인 생체정보. `[사용자 지시]`
- 임상·진단 능력을 주장하지 않는다 — **"연구용 신호처리 시스템"** (`docs/01_design.md` 1.2).
- PhysioNet 403 은 **조직 egress 정책**이다. 우회하지 않는다. `[사용자 지시]`
- 새 모델·손실·지표를 넣기 **전에** D 항목을 적는다 (`docs/19_record_keeping.md` §5).
- 기각한 가설과 틀린 예측을 지우지 않는다 — 그것이 이 과제의 산출물이다 (**D-32**).
- **세션 주제와 다른 파트로 넘어가면 먼저 알린다** — UI/UX 와 연구는 세션을 나눠 쓴다 (`docs/99_status.md` 10.2).

## 다섯 줄 — 전부 재발 이력이 있다

| 하려는 것 | 규칙 | 근거 |
|---|---|---|
| 프로세스 끝내기 | 대상 이름과 `kill` 을 **같은 줄에 두지 않는다.** 띄울 때 `$!` 를 적어 두면 이 문제가 없어진다 | O-15 ×2 · O-17 · O-18 · O-21 — **5 회** |
| 무인 실행 띄우기 | 띄우기 **전에** watchdog 확인(`list_triggers`), **락을 잡는 러너**로만 띄운다 | O-23 · O-24 · O-25 |
| 그림 만들기 | 저장 **전에** `pytest tests/test_figure_text.py`, 그리고 **눈으로 본다** | 마크다운 별표 **3 회** |
| 산출물 지우기 | tracked 산출물은 **새 것이 완성된 뒤에** 교체한다. 지워야 하면 먼저 커밋 | O-10 · O-20 |
| 무인 실행 중 커밋 | `git add -A` 를 **쓰지 않는다** — 경로를 적어 담는다. 도는 학습을 쓸어 담으면 epoch 1 짜리가 결과로 올라간다 | O-30 |
| 수치 옮기기 | 로그 마지막 줄이 아니라 **파일에서 읽는다**(`best.pt` 의 `best_metric`). **커밋 메시지도 「옮기기」다** | F-19 · F-26 · O-31 — **3 회** |

## 작업 직전 — `docs/17_checklists.md` 의 해당 절만 본다

| 트리거 | 절 |
|---|---|
| 프로세스를 끝낼 때 | §1 |
| 무인 학습·실험을 띄울 때 | §2 |
| 그림을 만들 때 | §3 |
| 자동 생성 문서를 고칠 때 | §4 |
| 산출물을 지우거나 덮어쓸 때 | §5 |
| 수치를 문서로 옮길 때 | §6 |
| 결론을 적을 때 | §7 |

## 커밋 전

`pytest tests/` — 611 개, 약 1 분. **push 하면 Actions 가 같은 것을 돌리고
화면 스크린샷 42 장을 올린다** (`docs/99_status.md`). 기록 무결성 검사가 여기 들어 있어
**빠진 F/D 기록과 낡은 보고서 수치를 잡는다** (`tests/test_repo_integrity.py`).
실제 학습을 도는 검사는 `slow` 로 빠져 있다 — `-m slow` 로 따로 돌린다.

## 길 찾기

| 무엇 | 어디 |
|---|---|
| 종합 보고서 (유일한 진입점) | `docs/91_report.md` |
| 발견 · 결정 · 사고 기록 | `docs/20_findings.md` · `21_decisions.md` · `22_incidents.md` |
| 실험 절차서 / 설계 근거 | `docs/02_procedure.md` · `01_design.md` |
| 현재 상태 · 인수인계 | `docs/99_status.md` |
| **UI/UX 파트** — 진입점 · 운영(발산→UD→검증) · 도구 대장 | `docs/ui/00_index.md` |
